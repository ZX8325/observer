/* ═══════════════════════════════════════════════════════════════════
   ③ Civ —— 文明原型 + 形态 + 倾向
   ═══════════════════════════════════════════════════════════════════
   职责：世界走到「文明」阶段时掷一次，决定这世界长出了什么样的文明。

   设计参考《群星》（Stellaris）的物种系统。文明分三层：

     ① 原型（你长什么样）—— **七种**，由**世界参数加权**决定
        水世界多出海类、剧毒大气多出虫类……
        但不是必然，只是概率高 —— 这样同一个世界玩两次能长出不同文明。
        ⚡ 外加一个抽签抽不到的「外来者」（权重恒为 0，
           只能被「界外投放」带出来，见第七节 3.5）。

     ② 形态（你住在哪）—— **八种**，其中**五种由玩家投了什么元素**决定
        海洋 / 大陆 / 山地 / 沙漠 / 极地 / 森林 / 地下 / 天空

     ③ 倾向（你怎么运转）—— **八种**，五档由**光暗值**定、三档由**本源**定
        和平 / 征战 / 求知 / 信仰 / 隐世 / 游牧 / 商业 / 工匠
        ★ 界面上这三行印成「原型 / 特质 / 倾向」——
          标签在 `ROW_LABELS`，**别手抄**（要印在四个地方）。

   ⚠️ 顺序很重要：**先定倾向，再抽原型**。
      因为「信仰」倾向会让灵能类权重翻倍 ——
      先有性格，再决定形态，因果才说得通。

   ⚡ 额外还有一个 **1% 概率的「修真彩蛋」**（只有一种形态：修真）。
      它们不是普通文明，是"这东西不该出现在这里"的违和感。
      ⚠️ 它们**没有形态、也没有倾向**（`form` / `temper` 都是 null —— 归不了类），
         界面上那两行整个不出现。**那是有意的，别去补。**

   ⚠️⚠️ 这份头注释 2026-09-16 改过一次 —— 它原来写着"文明分两层""10 种原型"
      "倾向从 3 组对立轴里抽 1~2 组"，那是**三代以前的设计**，全都对不上了。
      （"3 组对立轴"那套就是 `world.civ.ethics`，**从来没被写入过**，
       界面标签「伦理」也是它留下的 —— 那天一起改成了「倾向」。）

   铁律 B：本文件不许出现 document / window / canvas。
   ═══════════════════════════════════════════════════════════════════ */

(function (root, factory) {
  var mod = factory(
    (typeof require !== 'undefined') ? require('./rng.js') : root.RNG
  );
  if (typeof module !== 'undefined' && module.exports) module.exports = mod;
  else root.Civ = mod;
})(typeof globalThis !== 'undefined' ? globalThis : this, function (RNG) {
  'use strict';

  /* ═══════════════════════════════════════════════════════════════
     七种文明原型
     ═══════════════════════════════════════════════════════════════

     base  = 基础权重（所有原型都从 10 开始，灵能类除外）
     bonus = 加权条件，每命中一条就往上加
     color = 文明光点的颜色（渲染层直接用）
     desc  = 写进传记的一句话

     ⚠️ 灵能类的基础权重只有 2 —— 它是最稀有的。
        但满足条件时可以涨到 40 左右（见下面的稀有度验证）。
     ═══════════════════════════════════════════════════════════════ */
  var PROTOS = [
    {
      key: 'mammalia', name: '兽类', color: '#F0C060', base: 10,
      folk: '兽群',
      desc: '兽群自海岸向内陆推进，聚居点沿着水脉一路排开',
      bonus: function (w) {
        var e = w.essence;
        return (e.energy >= 35 && e.energy <= 70 && e.land >= 40 && e.land <= 70) ? 30 : 0;
      }
    },
    {
      key: 'arthropoda', name: '虫类', color: '#D9A030', base: 10,
      folk: '巢群',
      desc: '巢群铺满地表，密到从高处看像一层雾',
      /* ⚠️⚠️ 2026-09-15：`能量 > 70` 换成了 `能量 > 45` —— 原来那半条**命中率 0.0%**。

         病根：**能量的天花板是 55**。
         大陆→生态 的门槛是 `能量 ≤ 60`（超了就卡住、判「未成形」），
         之后进生态还要再降 40 秒（−6.9）。
         实测文明阶段的能量范围：**13.5 ~ 55.1**，而且 **50 以上只有 0.3%**
         （45~50 堆了 23.9%，50 处有个悬崖）。
         **`能量 > 70` 一次都不可能成立。**

         ⚠️ 新门槛必须留在**悬崖以内** —— 量过：
             能量 > 45  → 26.8%（含剧毒）   ← 选的这个
             能量 > 48  → 18.6%
             能量 > 50  →  3.1%  ⚠️ 已经踩到悬崖了
         45 离悬崖有 5 点余量，是三个候选里最稳的。

         ★ 因果：**热的世界虫子多。** 和「剧毒大气」是同一边的两条路
           —— 都在说"这颗世界不好住，但巢群住得下"。 */
      bonus: function (w) {
        var n = 0;
        if (hasTrait(w, 'toxic')) n += 45;
        if (w.essence.energy > 45) n += 25;
        return n;
      }
    },
    {
      key: 'mollusca', name: '海类', color: '#5FC8D8', base: 10,
      folk: '潮民',
      desc: '潮民自潮间带演化而来，聚居点沿着海岸线排开',
      bonus: function (w) { return w.essence.water > 70 ? 45 : 0; }
    },
    {
      key: 'plantae', name: '植类', color: '#6FD86F', base: 10,
      folk: '林体',
      /* ⚠️ 别写回"植被连成整片" —— ending.js 的 lifePhrase 在生态旺盛时
         已经会写一遍"植被连成整片，动物随之出现"，两处会在同一段传记里
         撞成重复句（植类世界约每 20 局碰到一次）。这里换个说法讲同一件事。 */
      desc: '林体层层向上堆叠，最厚的地方阳光到不了地面',
      bonus: function (w) {
        return (w.essence.atmo > 60 && w.evo.layers.life > 0.8) ? 40 : 0;
      }
    },
    {
      key: 'lithoid', name: '石类', color: '#C8C8D0', base: 10,
      folk: '岩体',
      desc: '岩体以矿物为食，自行生长，移动速度以周期计',
      /* ⚠️⚠️ 2026-09-15：`水 < 25` 换成了 `水 < 55` —— 原来那条**命中率 0.0%**。

         病根：**水的地板是 42**（`choices.js` 的 `FLOOR.water`，
         外加 冷却→海洋 的门槛 `水 > 40`）。实测文明阶段的水 最低就是 42，
         **`水 < 25` 一次都不可能成立** —— 石类的 +45 是死代码。

         新上限 55 是量出来的（`_proto_probe.js`，641 局走到文明）：
             大地>75 且 水<45  →  3.6%
             大地>75 且 水<50  →  5.5%
             大地>75 且 水<55  →  7.8%   ← 选的这个
             大地>75（不限水） → 15.3%
         选 55 是为了保住"干燥"这层叙事（水多就成了湿地，不是石头的世界），
         又不至于像 45/50 那样太稀。

         ★ 因果仍然成立：**多山 + 不潮湿 = 石头的地盘**。
           而且和「兽类」（大地 40~70）**互补** —— 中间 70~75 留空。 */
      bonus: function (w) {
        var e = w.essence;
        return (e.land > 75 && e.water < 55) ? 45 : 0;
      }
    },
    {
      key: 'psionic', name: '灵能类', color: '#C8B4FF', base: 2,
      folk: '意群',
      desc: '意群的意识可以脱离躯体行动，精神场在大陆上方交织',
      /* ⚠️⚠️ 2026-09-15：`能量 > 80` 换成了 `能量 > 45` —— 原来那半条**命中率 0.0%**。

         病根和「虫类」那条**完全一样**：能量的天花板是 55，80 够不着。
         （见上面虫类那条的完整说明。）

         ⚠️ **这条改动的分量比虫类那条重**：灵能类的**基准只有 2**
         （不是 10），所以它比谁都更依赖加成。原来两条半里
           · `双星 +18`  —— 只有 2.0% 的局有双星
           · `能量 > 80` —— **死**
           · `光暗 > 30` —— 真的够得着，但那要玩家投光种（探针量不到）
         所以它实测份额只有 **2.1%**，是七个物种里最低的。

         ⚠️ 和虫类**用同一个门槛**是有意的：两条都在讲"能量高"，
            而且 45 是悬崖以内余量最大的那个数。
            **代价**：两句会同时成立（都是"热世界"）—— 但它们的
            另一半判据（剧毒 / 双星）和基准分（10 / 2）完全不同，
            不至于长得像。 */

         /* ⚠️ `round >= 3` 仍然是**死代码** —— `world.round` 全项目从没被赋过值
            （第 2 轮「继续演化」还没做，见 CLAUDE.md 第十节第 11 步）。
            留着是因为那一轮做出来它就活了。 */
      bonus: function (w) {
        var n = 0;
        if (hasTrait(w, 'binary')) n += 18;
        if (w.essence.energy > 45) n += 12;
        if (w.light > 30) n += 15;      // 光之力偏强 → 灵能类更常见
        if (w.round >= 3) n += 10;
        return n;
      }
    },
    {
      /* ── 人类 ── ★ 2026-09-12 加的
         定位：**四项本源都不极端的世界，才长得出人类。**

         为什么这么定：另外六个物种，每一个都在回答"什么样的世界长出什么"。
         而所有走极端的世界都被专精的物种抢走了 ——
         水世界有海类、石头世界有石类、毒气世界有虫类、双星世界有灵能类。
         **剩下那颗什么都不极端的星球，最后长出的是人类。**
         这也正是地球在宇宙里的位置。

         ⚠️ 加权卡在 45~65 这段窄区间里是**有意的**：
            放宽到 40~70 的话，它在近一半的世界里都吃满 +40，
            会把别的物种直接挤掉。改这个数之前先跑 _ending_test.js 看分布，
            目标份额是 10% 上下，和别的物种差不多。 */
      key: 'humanoid', name: '人类', color: '#E0C8A8', base: 10,
      folk: '人族',
      desc: '人族散居在大陆各处，聚落之间来往不断，没有一处能号令全部',
      bonus: function (w) {
        var e = w.essence, n = 0;
        if (inRange(e.energy, 45, 65)) n++;
        if (inRange(e.water,  45, 65)) n++;
        if (inRange(e.atmo,   45, 65)) n++;
        if (inRange(e.land,   45, 65)) n++;
        return n >= 3 ? 40 : 0;
      }
    },

    /* ═══════════════════════════════════════════════════════════════
       ★★ 外来者 —— 第 8 个物种，但**抽签永远抽不到它** ★★
       ═══════════════════════════════════════════════════════════════
       2026-09-14 新增。

       ── 为什么它是成立的 ──
       这个游戏每个物种都在回答「**什么样的世界长出它**」。
       当年「机械类」被砍掉，就是因为它答不上这道题 ——
       它是造出来的，不是长出来的。

       而外来者**答得上**：

           没有世界能长出它。它是被带来的。

       同一个道理，换了个位置就讲通了。

       ── 为什么 base 是 0 ──
       它只能由「界外之物投放种子」那条线强制带出来（见 evolution.js 的
       `outsiderRolled`，以及 `roll()` 里的强制分支）。
       正常抽签**一次都不该抽到它** —— 不然"它是被带来的"这个前提就塌了。

       ⚠️⚠️ `base: 0` + `bonus` 恒为 0 = 权重 0，`RNG.weighted` 永远抽不到。
           但那样会撞红 `_ending_test.js` 里「每个物种都必须至少抽到一次」
           那条断言（它本来是防"权重被调到永远不出现"的）。
           所以加了 `forced: true` 这个标记，让那条断言**显式豁免**它，
           并另配一条反向断言（"forced 的物种权重必须是 0"）。
           —— 把"有意为之"写出来，比放松测试强。

       ── 颜色 ──
       品红。现有十个物种/形态里**一个品红都没有**（暖金/琥珀/青蓝/翠绿/
       灰白/紫白/亚麻 + 金/淡银青/暗铜），一眼就能认出来。
       "不属于这个世界的颜色"正是要的效果。 */
    {
      key: 'alien', name: '外来者', color: '#F07AC8', base: 0,
      forced: true,          // ← 只能被强制投放，不参与正常抽签
      folk: '外来者',
      desc: '他们不是这颗星球上长出来的。此地的环境没有参与他们的形成',
      bonus: function () { return 0; }
    }
  ];

  /* ═══════════════════════════════════════════════════════════════
     ⚡ 修真彩蛋 —— 1% 概率抽出来的**一条独立的线**
     ═══════════════════════════════════════════════════════════════
     ★ 2026-09-12：从"一颗蛋"改成"一个格子，里面三种形态"。
     ★★ 2026-09-16：**三种砍回一种** —— 精灵和矮人整个删掉了 ★★
        原话是：「删除矮人精灵，彩蛋只保留修仙，不按照既定的规制来，
        专门写一条修仙世界的事件库，因为做了精细化处理所以不需要那么多种族」。
        ⇒ 与其把内容摊成三份薄薄的，不如把**一条线做深**。

     **为什么奇幻分支必须走这条路，不能当正常物种**（记下来，
     免得以后有人又想加精灵 / 矮人 / 半兽人 / 龙族）：
     正常物种每一个都要回答"什么样的世界长出它"，而
       · 森林已经是**植类**（大气 > 60 且生态 > 0.8）和**林体**（森林文明）的地盘
       · 山地矿脉已经被**石类**（大地 > 75 且水 < 55）和**山地文明**占着
     它们**没有位置可站**，最后只能写成"概率随机出现" ——
     那等于承认它们不是这个世界长出来的，是硬塞进去的。
     而且会顺手挤掉现有物种的出场率。

     ⚠️⚠️ 原来那三条铁律（① 观察者必须困惑 ② 绝不解释它们是什么
        ③ 不许有正常的历史行）**在修真这条线上作废了**（2026-09-16）——
        要的是"真写修仙世界"：雷劫、功法、飞升前夜。
        ⚠️ **但别处的规矩一个字都没松**：整个游戏（另外八个物种、
        岔路口、命运、界面）**照旧不许出现修仙词**。
        这条线是全项目**唯一**解禁的地方，豁免名单在
        `_style_test.js` 的 XIUXIAN 那段。

     ⚠️⚠️ **删掉两个 key 的代价**（记着，因为它是静默的）：
        `elven` / `delving` 不再存在于任何一张表里 ——
        `Civ.byKey('elven')` 会兜底成 `PROTOS[0]`（**兽类**），
        `speciesOf('elven')` 会退回通用池（**人类历史行**），
        都不报错。但**档案库存的是成品**（结局文案是当时算好的字符串）、
        而这游戏是纯离线 H5、**没有"进行中的存档"** ——
        所以只有"页面没关就更新了代码"这一种情况会撞上，接受。

     ⚠️ 概率：**1%**（`ANOMALY_CHANCE`）。原来三种形态时各拿 1/3，
        只剩一种之后**自然回到 1%**（2026-09-16 拍板）。
     ═══════════════════════════════════════════════════════════════ */
  var ANOMALIES = [
    {
      key: 'cultivation', name: '修真', color: '#FFD870',
      folk: '修士',
      /* ★★ 2026-09-16：这一段**重写过** ★★
         原来写的是「观测到异常。此界生灵的发展路径无法用已知框架归类…」——
         那是"观察者看不懂"那套写法（铁律①②）。要的是
         **真写修仙世界**，所以现在直接写他们是什么样。

         ⚠️ 它有**三个读者**，改之前先看一遍：
           ① 结局卡「文明」那一节（`ending.js` 的 `civPhrase` 读它）
           ② 传记里「文明在其上崛起：…」那一句（同一个函数）
           ③ `_style_test.js` 的文风扫描 —— **修仙词在这里是豁免的**
              （全项目唯一一处，见那张 XIUXIAN 表和豁免名单）

         ⚠️ 句尾**不带标点**更好看：`civPhrase` 会把句尾标点剥掉再拼。
         ⚠️ 它同时是"这条线到底写成什么样"的样板 ——
            历史行 / 征兆 / 事件都该是同一个调子。 */
      desc: '此界生灵不靠血肉活着。他们把身体一层层炼开，' +
            '炼到能离地，再炼到不必落地。天上落雷的时候，他们站着不动。'
    }
  ];

  /* ═══════════════════════════════════════════════════════════════
     八种文明形态 —— 「他们住在哪」
     ═══════════════════════════════════════════════════════════════

     ⚠️ 形态和物种是**两个正交的维度**：
          物种 = 他们长什么样（兽类 / 虫类 / 石类…）
          形态 = 他们住在哪（海洋 / 地下 / 天空…）
        所以「海类 · 地下文明」这种组合是成立的（一群海类住进了地底）。

     ★ 重点：**八个里五个由「投放了什么元素」决定**。
        这是元素投放第一次能影响结局 —— 之前它只影响数值，不影响长出什么文明。

        山地 ← 土石投放      极地 ← 冰晶投放      森林 ← 光种投放
        地下 ← 暗种投放      天空 ← 气团投放
        （你的需求里写的是"山脉/植被投放"，游戏里没有这两种元素，
          分别对应到「土石」和「光种」—— 光种加速生态，长出来就是植被。）
     ═══════════════════════════════════════════════════════════════ */
  var FORMS = [
    {
      key: 'ocean', name: '海洋文明', color: '#5FC8D8', base: 10,
      desc: '他们把聚居点建到了洋面上，船队往来于彼此之间',
      bonus: function (w) {
        var n = 0;
        if (w.essence.water > 70) n += 40;
        n += tallyBonus(w, 'water', 3, 25);     // 水珠投放
        return n;
      }
    },
    {
      key: 'continent', name: '大陆文明', color: '#F0C060', base: 10,
      desc: '他们把聚居点与耕地铺在内陆，从一处走到另一处要几个月',
      bonus: function (w) { return w.essence.land > 70 ? 40 : 0; }
    },
    {
      key: 'mountain', name: '山地文明', color: '#C8C8D0', base: 10,
      desc: '他们把堡垒嵌进山脊，索道横跨峡谷',
      bonus: function (w) { return tallyBonus(w, 'stone', 3, 45); }
    },
    {
      key: 'desert', name: '沙漠文明', color: '#D8B86A', base: 10,
      desc: '他们把聚居点建在有水的地方，沙下埋着更早的遗迹',
      /* ⚠️⚠️ 2026-09-15：`能量 > 70` 且 `水 < 35` —— **两个子句都够不着**。
         这条是 `_ending_test.js` 新加的 A2 段（可达性扫描）**自己抓出来的** ——
         当时报的是石类/虫类/灵能类三条，这条是扫描顺手找到的第四条。

         · 能量的天花板是 **55**（大陆→生态 的门槛 `能量 ≤ 60`，再降 40 秒）
         · 水的地板是 **42**（`choices.js` 的 `FLOOR.water`）
         所以 `> 70` 和 `< 35` 一个都不成立。**沙漠文明从来没出现过。**

         ── 新的两个数都是量出来的（`_proto_probe.js`，641 局走到文明）──
             能量>45 且 水<50  →   4.4%
             能量>45 且 水<55  →   7.5%   ← 选的这个
             能量>45 且 水<60  →  10.0%
             能量>40 且 水<55  →  12.0%
         `能量 > 45` 和「虫类 / 灵能类」用的是同一个数（都是"热"这个信号），
         而且离 50 那个悬崖有 5 点余量。水取 55（下面约三成五）—— 够干，
         又不至于像 50 那样只剩 4.4%。

         ★ 因果没变：**又热又干 = 沙漠。** */
      bonus: function (w) {
        var e = w.essence;
        return (e.energy > 45 && e.water < 55) ? 45 : 0;
      }
    },
    {
      key: 'polar', name: '极地文明', color: '#E8F0FF', base: 10,
      desc: '他们把聚居点建在冰盖以下，天顶常年悬着极光',
      bonus: function (w) { return tallyBonus(w, 'ice', 3, 45); }
    },
    {
      key: 'forest', name: '森林文明', color: '#6FD86F', base: 10,
      desc: '他们把居所架到树冠之间，彼此之间用藤桥连接',
      bonus: function (w) {
        var n = tallyBonus(w, 'light', 3, 35);
        if (w.evo.layers.life > 0.8) n += 25;
        return n;
      }
    },
    {
      key: 'under', name: '地下文明', color: '#8A5AC8', base: 10,
      desc: '他们把聚居点沉进地底，靠岩层里的热取暖',
      bonus: function (w) { return tallyBonus(w, 'dark', 3, 45); }
    },
    {
      key: 'sky', name: '天空文明', color: '#D8DCE4', base: 10,
      desc: '他们把聚居点悬在云层之上，往返全靠飞艇',
      bonus: function (w) { return tallyBonus(w, 'air', 3, 45); }
    }
  ];

  /* ═══════════════════════════════════════════════════════════════
     八种文明性格 —— 「他们怎么运转」
     ═══════════════════════════════════════════════════════════════

     五档由**光暗值**决定（这是光暗值的第四个作用了），三档由本源决定。

     ⚠️ 光暗值是会**每秒回归 0** 的，所以性格反映的是
        "世界走到文明那一刻，玩家最近有没有在干预"。
        ——你不碰它，就是和平；你一直堆光种，就是信仰。

     ═══════════════════════════════════════════════════════════════
     ★★ 这八个数是「权重」，不是「概率」★★（2026-09-15 补记）

        · 基准分全是 10，条件是**往上加**，不是"只有满足才可能出"
        · 所以**任何条件下八种都可能出** —— 只是概率不一样
        · 这是项目老原则（决策 #11「只推概率，不保证」），
          但只在 `choices.js` 和 `lean` 那两处写了，**这张表一直漏着**

     ⚠️⚠️ 由此带来一个**必须知道的后果** ⚠️⚠️
        基准分 10 是**白送的份额**：某个性格的条件完全不成立时，
        它照样能拿走 `10 / 全部权重和`。

     ★★ 2026-09-15 实测（`node _temper_probe.js`）—— **五个性格的加权从来没生效过**：

       和平 100% 命中 ｜ 商业 37.9% ｜ 工匠 23.5%
       ⚠️ 征战 / 求知 / 信仰 / 隐世 **0%** —— 它们全挂在**光暗值**上，
          而光暗值每秒回归 0，不投元素的玩家永远推不到那几档
       ⚠️ 游牧 **0%** —— ★ ~~构造上不可能~~
       ★★ 2026-09-17 更正（**这一行现在是过时的**）：当时量到的 0% 是真的，
          但归因错了 —— 那不是"构造上不可能"，是**口径打架**
          （门槛读"有效大地"，判据读"全局大地"）。**"构造上不可能"这句话
          本身是错的**，判据也已经换成「水 < 70 且大地 30~55」，
          现在命中约 **21.8%**。详见 `nomad` 那一行的长注释。

       所以「和平偏高」的真正根子不是它自己的数，
       是**别人根本没法发力**。光把和平往下调，只是把白送的那份摊匀。
     ═══════════════════════════════════════════════════════════════ */
  var TEMPERS = [
    {
      key: 'peace', name: '和平', base: 10,
      desc: '他们向外扩展得很慢，边界几个周期才挪动一次',
      /* ★ 2026-09-15：加权 **50 → 35**（总权重 60 → 45）。
         2026-09-15 提的：「和平 60 偏高，建议降到 45 左右」。

         ⚠️⚠️ 但**病根不在这个数** —— 实测（`_temper_probe.js`，600 局）：
             和平的条件命中率 **100%**（光暗值不碰就是 0，正好落在 ±25 里），
             而征战 / 求知 / 信仰 / 隐世 **全是 0%**（那几档要光暗值推开）。
             所以它偏高是因为**别人根本没机会发力**，不是因为它自己太大。

         实测改前 → 改后（占走到文明的局）：
             和平 36.3% → **29.8%**      商业 16.4% → 18.6%
             工匠 14.2% → 15.0%          求知  6.8% →  8.7%
         方向对，但它**只是把白送的那份摊匀** —— 见表头那段。 */
      bonus: function (w) { return inRange(w.light, -25, 25) ? 35 : 0; }
    },
    {
      key: 'war', name: '征战', base: 10,
      desc: '他们扩张迅速，与邻接势力的冲突没有停过',
      // 征战型会把「世界寂灭」的判定放宽 —— 一个四处开战的文明更容易把自己打没
      doomProne: true,
      bonus: function (w) { return inRange(w.light, -70, -25) ? 50 : 0; }
    },
    {
      key: 'seek', name: '求知', base: 10,
      desc: '他们不断试探这颗星球的边界与规则',
      bonus: function (w) { return inRange(w.light, 25, 70) ? 50 : 0; }
    },
    {
      key: 'faith', name: '信仰', base: 10,
      desc: '他们为光源建起神殿，一切作息围着它安排',
      bonus: function (w) { return w.light > 70 ? 55 : 0; }
    },
    {
      key: 'hermit', name: '隐世', base: 10,
      desc: '他们藏了起来，不向外踏出一步',
      bonus: function (w) { return w.light < -70 ? 55 : 0; }
    },
    {
      key: 'nomad', name: '游牧', base: 10,
      desc: '他们不建固定的聚居点，随着季节迁徙',
      /* ═══════════════════════════════════════════════════════════
         ★★ 2026-09-15：判据整个换掉了（已拍板）★★

         ── 换之前是什么，以及为什么必须换 ──
         原来写的是 `w.essence.land < 30`，**命中率实测 0.0%**（600 局一次没发力）。

         ⚠️ 而且本文档/注释里一度把这个说成「条件**构造上不可能**成立」——
            **那句话是错的**。真正的机制是**口径打架**：
              阶段门槛（evolution.js 的 blockedReason）读的是
                **有效大地 = 全局 + 元素投放加成**（Elements.averageBoost）
              而这一行的判据读的是 **全局大地 `w.essence.land`**
            所以堆一堆土石**理论上**能把它顶出来 —— 但那个路径又窄又荒谬
            （一个土石斑对全局只贡献约 0.7，还顶着 MAX_DROPS = 24；
              而且方向是反的：拿"加陆地"的元素换"陆地太少"的性格）。

         ── 现在是什么 ──
             水 < 70  且  大地 30 ~ 55

         ★ **两个数各有各的来历，都不是拍的**：

           ① **水 < 70** —— 定下的。两重作用：
              · 和「商业」（水 > 70）**严格互补**，两者永远不会同时成立
                （这正是一开始担心的"游牧 × 商业 逻辑打架"，
                  这次是用**边界**解决，不是打补丁）
              · 叙事上也对：草原不该是沼泽

           ② **大地 30 ~ 55** —— 给的是 30~50，★ **我按实测量调成了 55**。
              区间上限是拿命中率对齐出来的（`_nomad_probe.js`，700 局，文明阶段）：

                  大地 30~50 且 水<70  →  15.1%
                  大地 30~55 且 水<70  →  21.8%   ← 选的这个
                  大地 30~60 且 水<70  →  30.0%
                  （对照：工匠 23.2%   商业 37.0%）

              选 55 的理由：让三个"本源性格"的稀有度拉成 **37 / 23 / 22**，
              比较匀。选 50 的话游牧会是最稀少的那一个（15.1%）。

         ── 叙事 ──
         **陆地不多不少 = 大片草原 → 逐水草而居。**
         和「工匠」（大地 > 70 = 山地矿脉）形成两极，中间 55~70 留空
         —— 那一段"不够特别"，谁都不加。

         ⚠️ 加权 **+45 没动**（当初只提了判据）。动两个数的话，
            以后量出来的变化分不清是哪一个造成的。

         ⚠️ 测量口径：`_nomad_probe.js` / `_temper_probe.js` 都**不投元素**，
            所以这两个数是"**完全不投元素的下限**"。真投元素的玩家
            （尤其土石）会把大地推高，分布会更宽。
         ═══════════════════════════════════════════════════════════ */
      bonus: function (w) {
        return (w.essence.water < 70 && inRange(w.essence.land, 30, 55)) ? 45 : 0;
      }
    },
    {
      key: 'trade', name: '商业', base: 10,
      desc: '他们把贸易网铺开，货物从一处流到另一处',
      /* ⚠️⚠️ **2026-09-15：这里原来打算加一个「且大地 ≥ 30」的补丁，
         用来把"游牧 × 商业同时成立"错开 —— 实测之后决定**不打**。**
         （当时提的是：「游牧 + 商业同时高 → 逻辑打架，建议错开」）

         ── 为什么不打 ──
         实测（600 局）：★ **游牧×商业同时成立 = 0.0%**，一局都没有。
         加那个条件 = 加一段**几乎永远为真**的判断 ——
         判据是决策 #61：**删掉它，行为会不会变？** 不会。
         那就是维护成本，而且会误导下一个人以为这里有讲究。

         ⚠️ 2026-09-15 更正：这里原来写的是"构造上不可能发生" ——
            那个前提（文明阶段的大地一定 > 30）**被上面游牧那一行的更正推翻了**。
            严格说，投了一堆土石的局里两者**可以**同时成立。
            但那个窗口极窄（要 24 个土石、还得刚好压在水 > 70 的世界），
            **没有实际影响** —— 所以"不打这个补丁"的结论**不变**。

         ⚠️ 要恢复的话，**先证明那个窗口宽到值得处理**（把上面那条修好再说）。 */
      bonus: function (w) { return w.essence.water > 70 ? 40 : 0; }
    },
    {
      key: 'craft', name: '工匠', base: 10,
      desc: '他们留下的建筑，规模一处比一处大',
      bonus: function (w) { return w.essence.land > 70 ? 40 : 0; }
    }
  ];

  /* ═══════════════════════════════════════════════════════════════════
     四属性（军事 / 科技 / 生产 / 文化）  ★ 2026-09-15 新增

     文明阶段的四个数。开局算一次，之后由小事件推拉（见 civEvents.js 的
     `OPTION_ATTR`），最后决定命运（见 ending.js）。

     ⚠️⚠️ 两条**结构上**的规矩，都是 2026-09-15 定的：

       ① **只有性格给文化，物种不给。**
          原话：「文化的初始属性不靠种族写靠特性写，类似求知这种可以多给一点」。
          所以 `ATTR_SPECIES` 里**根本没有 cul 这一项**（不是漏写）。

       ② **修真彩蛋没有性格。**
          拿不到性格加成，只有种族基准 —— 这是天然自洽的，不用特判。

     ⚠️⚠️ 这两张表里的数字**全部是"未调"的**（见 CLAUDE.md 第十节
        「待改的数值账」）。先按这套把框架跑通，调数值统一留到最后。
     ═══════════════════════════════════════════════════════════════════ */

  var ATTR_KEYS  = ['mil', 'sci', 'prod', 'cul'];
  var ATTR_NAMES = { mil: '军事', sci: '科技', prod: '生产', cul: '文化' };

  /** 全 0 —— 查不到时的兜底，也是"没有任何加成"的字面意思 */
  var ATTR_ZERO = { mil: 0, sci: 0, prod: 0, cul: 0 };

  /**
   * 种族基准。键和 `PROTOS` 一一对应。
   * ⚠️ 这里**没有 cul** —— 见上面规矩 ①。
   * ⚠️ `_civEvents_test.js` 有三条断言：**每个物种（含修真彩蛋）都得在这里有一行**、
   *    八个性格都得在 `ATTR_TEMPER` 里有一行、以及**这张表里不许出现 cul**。
   *    漏一个就是静默的全 0（和"加了物种忘了写历史行"是同一类坑）。
   */
  var ATTR_SPECIES = {
    humanoid:    { mil: 1, sci: 1, prod: 1 },   // 类人：四项都不极端 → 均衡
    mammalia:    { mil: 2, sci: 0, prod: 1 },   // 兽类：能打
    arthropoda:  { mil: 1, sci: 0, prod: 2 },   // 虫类：数量就是生产力
    mollusca:    { mil: 0, sci: 1, prod: 2 },   // 海类：潮汐里讨生活
    lithoid:     { mil: 1, sci: 1, prod: 1 },   // 石类：地质时间，不急
    psionic:     { mil: 0, sci: 2, prod: 1 },   // 灵能类：不靠肉体
    plantae:     { mil: 0, sci: 1, prod: 2 },   // 植类：扎根、蔓延、极稳
    alien:       { mil: 1, sci: 2, prod: 0 },   // 外来者：带着外面的东西来，不扎根
    /* ⚠️⚠️ **修真这一行三项和是 4，比别的物种多 1** —— 这是
       **2026-09-15 拍板保留的，别"修"它**。

       起因：除了它，所有正常物种的三项和**全是 3**（那张表就是按
       "三点怎么分"设计的 —— 种族只决定**往哪偏**，不决定总量）。
       我一度提议把它压回 3，理由是"彩蛋没有性格加成、本来就低"。

       原话是：「**矮人修真的数值不压**」—— 保留 4。
       （2026-09-16 矮人已删，这条数值约定只对修真有效了。） */
    cultivation: { mil: 2, sci: 2, prod: 0 }    // ⚡ 修真：攻伐 + 玄门
  };

  /**
   * 性格加成。★ **只有这张表有 `cul`。**
   *
   * ⚠️ 文化的方向是定下的「**类似求知这种可以多给一点**」——
   *    所以"精神向"的四种（求知 / 信仰 / 工匠 / 隐世）culture 给得高，
   *    征战 / 商业 / 游牧 给 0。
   *    ⚠️ 具体数字待调，这里只是把**方向**钉下来。
   */
  var ATTR_TEMPER = {
    peace:  { mil:  0, sci: +1, prod: +1, cul: +1 },
    war:    { mil: +2, sci:  0, prod:  0, cul:  0 },
    seek:   { mil:  0, sci: +2, prod:  0, cul: +3 },   // ★ 求知，文化多给
    craft:  { mil:  0, sci:  0, prod: +2, cul: +2 },   // ★ 工匠，文化多给
    faith:  { mil: -1, sci: +1, prod:  0, cul: +3 },   // ★ 信仰，文化多给
    trade:  { mil:  0, sci: +1, prod: +1, cul:  0 },
    nomad:  { mil: +2, sci: -1, prod: -1, cul:  0 },   // ★ 军事是正的（马上民族）
                                                       //   ⚠️ 2026-09-16 mil +1 → +2（定下的）——
                                                       //   配合同一轮 14 处 `lean` 的修正，
                                                       //   把"军事垫底"这件事往上抬一点。
                                                       //   见 civEvents.js 的 LEAN_PUSH 那一段
    hermit: { mil: -1, sci: -1, prod: -1, cul: +2 }    // ★ 隐世，文化多给
  };

  /**
   * 算一个文明的**初始**四属性。只在进入文明那一刻调一次。
   *
   * @param {string} protoKey  `world.civ.proto`
   * @param {string} temperKey `world.civ.temper`（修真彩蛋是 null）
   * @returns {{mil:number, sci:number, prod:number, cul:number}}
   */
  function initialAttrs(protoKey, temperKey) {
    var sp = ATTR_SPECIES[protoKey] || ATTR_ZERO;
    var tp = (temperKey && ATTR_TEMPER[temperKey]) || ATTR_ZERO;
    return {
      mil:  sp.mil  + (tp.mil  || 0),
      sci:  sp.sci  + (tp.sci  || 0),
      prod: sp.prod + (tp.prod || 0),
      /* ★ 文化**只有性格给** —— `sp.cul` 永远不存在（见规矩 ①）。
         写成 `tp.cul || 0` 是有意的：物种那半边不该有文化。 */
      cul:  tp.cul || 0
    };
  }

  /* ═══════════════════════════════════════════════════════════════════
     四属性的**规则**（怎么涨、怎么落）  ★ 2026-09-15
     ═══════════════════════════════════════════════════════════════════

     ⚠️⚠️ 下面每一个数都是**未调**的（见 CLAUDE.md 第十节「待改的数值账」）。
         先把框架跑通，调数值统一留到最后 —— 决策 #105。

     ── 两件事：① 文化怎么自己涨 ② 属性怎么减缓压力 ──
     ═══════════════════════════════════════════════════════════════════ */
  var ATTR_RULES = {
    /* ── ① 文化的自然增长 ──
       原话是：「文化差异化，我想了下如果离心到达一定阶段文化就不再发展，
       离心过高文化倒退」。

       ★ **按进度连续涨**，不按纪元跳 —— 这是选定的做法，别改回去：
         · 被切断的文明拿到的是**连续值**，不会因为"没活到第四纪元"归零
         · 不耦合纪元结构（纪元是给读者看的时间刻度，不是记账单位）
         · 活满一局 = culGain，**和物种无关** ——
           文化的差异来自起点（性格）、事件、以及**离心有没有把它掐掉** */
    /* ⚠️⚠️ 原来写的是 4，**2026-09-15 实测之后降到 1** —— 这是量的，不是拍的。

       ── 病根 ──
       文化是四个数里**唯一会自己涨的**，那几点是白送的；
       另外三项得靠"种族基准 + 小事件"一点点挣（均值各约 1.4 / 1.7）。
       所以文化一旦涨得多，**「哪个属性最高」就永远轮不到别人** ——
       而命运现在是按"最高的那一项"分方向的（见 ending.js 的 lifeAxis），
       于是六成三的局全挤在文化那一支。

       ── 实测（同 2400 颗种子，只改这一个数）──

         culGain │ 军事  科技  生产  文化  均衡 │ 最高的一条命运
         ────────┼────────────────────────────┼─────────────────
            4    │  8.6   8.3  15.7  63.5   3.9 │ 融合 25.8%
            2    │  9.7  12.0  24.7  46.7   6.9 │ 融合 20.0%
         ★  1    │ 10.1  14.9  29.5  36.0   9.5 │ 融合 15.9%
            0    │  （没量 —— 文化就完全不长，违背"文化差异化"的设计）

       ── 为什么选 1，不选 0 ──
       文化的**差异**主要来自两处，跟这个数无关：
         ① 起点：性格给 0~3（求知/信仰 +3，征战/商业/游牧 0）
         ② ★ 离心的抑制：离心到 60 停涨、到 85 倒退（见 culHoldRift）
       这个数只是给"活到最后的文明"补一个底。
       留 1 的意思：**它是"活下来了"的标记，不是可以拿来赢的东西。**

       ⚠️ 这个数**还要接着调**（见 CLAUDE.md 第十节「待改的数值账」）。 */
    culGain:       1,     // 编年史走满，文化净涨这么多
    culHoldRift:  60,     // 离心 ≥ 这个数 → **停涨**
    culFallRift:  85,     // 离心 ≥ 这个数 → **倒退**
    culFallRate:  -2,     // 倒退档按进度算的净增速（负的）

    /* ── ② 属性 → 压力减缓 ──
       原话是：「压力值可以加一个设定，如果某个值高于一定点
       可以减缓对应压力值」。

       ★ 映射和三条压力线的语义对齐：
           生产 → 消耗（家底厚就吃得慢）
           科技 → 失衡（改造地表的本事）
           军事 → 离心（压得住内部）
       ⚠️ **文化不减缓任何压力**，这是有意的：文化管"活成什么样"，
          不管"活不活得下去" —— 和「抵抗判定不算文化」是同一条分工。 */
    reliefAttr: { drain: 'prod', unbal: 'sci', rift: 'mil' },
    /* ⚠️ 原来写的是 4 —— **实测之后降到 2**（2026-09-15）。

       ── 为什么降 ──
       四属性的终值均值只有 **1.9 / 2.6 / 3.5 / 3.5**（见 `_temper_probe.js`），
       所以 `-4` 这个门槛把绝大多数文明都挡在外面：
       三条线平均只减掉 **0.94 / 0.48 / 0.13**（合计 1.5，而预算是 10）。
       等于这个机制**基本没生效** —— 玩家一路把生产堆上去，压力一点都不减。

       ── 代价（量的，不是估的）──
       让它真的发力 = 游戏变简单。用 `_balance_probe.js` 同一把尺子扫的：

           reliefFrom   ③默认玩法崩溃率
              4（原）        36.2%
              3             35.2%
             ★2             31.7%
              1             26.6%  ← 快贴着报警区间的下沿（25%）了

       ★ 选 2：机制**真的在减**了，而 31.7% 仍在 K 节报警区间（25%~70%）当中。
       ⚠️⚠️ 但要知道**代价是什么**：默认难度从 37% 又掉了 5 个点，
          而设计目标本来是 45%（决策 #94 已经接受过一次 37%）。
          **这个账记在 CLAUDE.md 第十节「待改的数值账 ③」，调的时候一起看。** */
    reliefFrom:  2,       // 属性超过这个数才开始减缓
    reliefPer:   1,       // 每多 1 点，对应那条线减多少压力
    reliefMax:  10,       // ★ 三条线**加起来**的上限（⚠️ 红线：不许减到 0）
    floor:       0,       // 属性下限（文化倒退也跌破不了这里）

    /* ── ③ 结局怎么读这四个数 ──
       原话是：「**以默认 10 为满值，但是超过 10 了还是继续写 11、12**，
       多的值**不参与结局设定**了，只是显示而已」。

       ⚠️⚠️ 注意它**只管结局**：
          · 算命运时（`ending.js` 的 `lifeAxis`）超过 `endCap` 的按 `endCap` 算
          · **界面上照原样显示**（11 就写 11，不是 10）——
            那是"这个文明强到超出标尺了"的意思，抹掉就没味道了
       别把这两个地方弄反。 */
    endCap:  10,
    /* 最高和最低差 ≤ 这个数 → 算「四项均衡」（走「轮回」）。
       ⚠️ 待调 —— 见 CLAUDE.md 第十节「待改的数值账」。 */
    evenGap: 1
  };

  /**
   * 一个文明的四属性。**唯一的入口** —— 别的地方不许直接读 `civ.attr`。
   *
   * ⚠️ 为什么要一层函数、不直接读字段：
   *    **老存档里没有 `attr`**（2026-09-15 之前存的）。读出来是 undefined，
   *    后面一加就变成 NaN，一路静默传到底。这里现算一份补上 ——
   *    算的就是"当初该是多少"，不是随便编一个。
   *
   * ⚠️ 它会**就地补写**回 `civ.attr`。对老存档来说这是自愈；
   *    对新世界来说那个字段本来就有，等于什么都没做。
   */
  function attrOf(civ) {
    if (!civ) return { mil: 0, sci: 0, prod: 0, cul: 0 };
    if (!civ.attr) civ.attr = initialAttrs(civ.proto, civ.temper);

    /* ⚠️ 顺手把**下限**夹一遍（四个数都不许低于 `floor`）。
       夹在这儿、而不是夹在每个写入点：写入点有**四处**
       （Civ.roll / 小事件 / 大事件 / 文化自然增长），
       各夹一遍迟早漏一处 —— 而漏掉的那处会一路静默传到底，
       最后变成界面上一个「文化 −2」。

       ⚠️ 所以界面上、结局判定里读这四个数，**都要走这个函数**，
          别直接摸 `civ.attr`。 */
    var f = ATTR_RULES.floor, a = civ.attr;
    if (a.mil  < f) a.mil  = f;
    if (a.sci  < f) a.sci  = f;
    if (a.prod < f) a.prod = f;
    if (a.cul  < f) a.cul  = f;
    return a;
  }

  /**
   * 四属性 → 三条线**各减多少压力**（返回的三个数都是正数，"该减这么多"）。
   *
   * ⚠️⚠️ 上限是「**三条加起来**」，不是「每条各自」 ⚠️⚠️
   *    这是那条红线（**不许把压力减到 0**）的落点。
   *    如果每条各自封顶，三个属性都高的时候能减掉三倍，
   *    有些物种就永远崩不了了 —— **物种被锁死是设计红线**
   *    （第一版 ENDURE 范围拉到 0.68~2.20 时就是这么栽的）。
   *    所以预算 `reliefMax` 在这里**一次分完**，先到先得。
   *
   * 用法：`c.relief = Civ.reliefFor(world.civ.attr)`（每帧算一次），
   *      然后 `civLore.js` 的 `lineShift(c, k)` 会把它算进偏移里。
   */
  function reliefFor(attr) {
    var out = { drain: 0, unbal: 0, rift: 0 };
    if (!attr) return out;
    var keys = ['drain', 'unbal', 'rift'];
    var left = ATTR_RULES.reliefMax;          // ★ 三条共用的那一份预算
    for (var i = 0; i < keys.length; i++) {
      var k = keys[i];
      var a = attr[ATTR_RULES.reliefAttr[k]] || 0;
      var r = Math.max(0, a - ATTR_RULES.reliefFrom) * ATTR_RULES.reliefPer;
      if (r > left) r = left;
      out[k] = r;
      left -= r;
    }
    return out;
  }

  /** 数一数某个元素投了几次，够数就给加权 */
  function tallyBonus(world, key, need, gain) {
    if (!world.dropTally) return 0;
    return (world.dropTally[key] || 0) >= need ? gain : 0;
  }

  function inRange(v, lo, hi) {
    return v >= lo && v <= hi;
  }

  /** 掷到「修真彩蛋」的概率（百分比）。
      ⚠️ 1% —— 和最早的修真彩蛋一模一样。
         2026-09-12~16 那阵子有三种形态、各拿 1/3（0.33%）；
         2026-09-16 砍回一种，这个数**自然回到 1%**（已拍板）。 */
  var ANOMALY_CHANCE = 1;

  /* ═══════════════════════════════════════════════════════════════
     掷骰
     ═══════════════════════════════════════════════════════════════ */

  /**
   * 决定这世界长出什么文明。
   *
   * ⚠️ 结果**完全由世界的种子决定** ——
   *    同一颗世界重玩一遍，长出来的文明是一样的。
   *    这跟"同一个种子必然产出同一个世界"是同一个道理：
   *    种子分享出去，别人能看到一样的结局。
   *
   * @param {object} world 世界档案（会被就地写入 world.civ）
   */
  function roll(world) {
    // 用种子派生一个独立的随机源。
    // 为什么不复用 worldgen 的那个：那边已经用掉了自己的序列，
    // 这里需要一条**固定起点**的新序列，才能保证重玩结果一致。
    var rng = RNG.makeRng((world.seedNum ^ 0x51C1F00D) >>> 0);

    /* ── ① 先掷「修真彩蛋」骰 ──
       ⚠️ 中了还要**再抽一次**从 `ANOMALIES` 里挑一个 ——
          现在表里只有一条，所以抽出来的一定是它；
          留着这一抽是为了"以后再加形态"时不用改这里
          （而且它照常消耗一次随机数，序列不会错位）。

       ★ 2026-09-14：前面多了一个 `!world.outsiderSeeded &&` ——
          **界外投放过的世界，异常骰直接跳过**。

          为什么必须这样：要的是「投放之后文明**必须**会变成
          投放的那种生物」。观测异常是**强制覆盖物种**的另一条路，
          两条路撞上时只能有一个赢。

          实测过一次（600 局）：投放判在异常后面的写法，
          **每 600 局就有 1 局**是"投放了种子、却长出修真" ——
          玩家刚看见"有东西落下来"，转头得到一个归不了类的东西，
          那条线当场就废了。

          ⚠️ 净效果：观测异常从 1% 变成 1% × 90% ≈ 0.9%
             （被投放的 10% 里不再掷异常骰）。
             `_ending_test.js` 那条"≈1%（±0.3%）"仍然过。

          ⚠️⚠️ 对**没被投放**的局，这一改**一个字节都不改**：
              `&&` 左边为真 → 右边照样求值 → `RNG.chance` 照常消耗一次随机数。
              （顺序反过来写就会让所有旧种子错位。） */
    if (!world.outsiderSeeded && RNG.chance(rng, ANOMALY_CHANCE)) {
      var an = ANOMALIES[Math.floor(rng() * ANOMALIES.length)];
      world.civ = {
        proto: an.key, protoName: an.name, color: an.color,
        /* ★ 2026-09-16 补的 `folk` —— 和上面正常物种那条对齐。
           ⚠️ 老实说：**今天它是用不到的**（彩蛋独占池，没有任何
              共用文案，`{folk}` 一个都不出现）。留着有两个理由：
              ① 档案的形状要和别的物种一致（`world.civ.folk` 是每个
                 物种都有的字段，缺了它属于"形状不同"）；
              ② 哪天彩蛋的文案里写了 `{folk}`，取不到会**静默**
                 退回「此界生灵」—— 不会报错，只是读起来不对。
           （`evolution.js` 的 `beginCiv` 读的就是这个字段。） */
        folk: an.folk,
        form: null, formName: null, formColor: null,
        temper: null, temperName: null,
        isEgg: true,
        /* ★ 彩蛋**没有性格** → 拿不到性格加成，只有种族基准。
           这是天然自洽的，不用特判（见 ATTR_TEMPER 上面那段注释）。 */
        attr: initialAttrs(an.key, null)
      };
      return world.civ;
    }

    /* ── ② 先掷性格 ──
       顺序很重要：性格会影响物种的权重（信仰型让灵能类翻倍），
       所以必须**先有性格、再决定形态**，因果才说得通。

       ★★ 性格现在完全由**世界自己的状态**决定，玩家没有菜单可以点 ★★

          · `world.light`（光暗值）—— 玩家投的光种 / 暗种攒出来的
                 −25~25 和平 / −70~−25 征战 / 25~70 求知
                 > 70 信仰 / < −70 隐世
          · 本源  —— 水<70 且大地 30~55 游牧 / 水 > 70 商业 / 大地 > 70 工匠
                 ⚠️ 2026-09-17 改：游牧原来是「大地 < 30」，**那是旧判据**，
                    实测命中 0.0%，早换掉了。见 `nomad` 那一行。

       ⚠️⚠️ 2026-09-14：原来这里还收一个 `world.route.temperBias`
           （玩家在第三道岔路口"直接点名"要哪种性格，推力 200）。
           那道岔路口整个删掉了，参数也跟着去掉 —— 详见
           `core/choices.js` 开头那段「第三道岔路口为什么删掉」。

       ⚠️ 这一改**会让旧种子长出来的性格变掉**（和"加物种"同一类副作用）。
          理由：光暗值的来源变了 —— 以前进文明那一刻会被岔路口
          改成 +15 / −32 / +42，现在不会了。 */
    var temper = drawOne(rng, TEMPERS, world);

    /* ── ③ 再抽物种 ──
       ★★ 界外投放过了 → **跳过抽签，直接就是外来者** ★★

       这是这只手唯一的入口：`PROTOS` 里外来者的 `base` 是 0、`bonus` 恒为 0，
       **正常抽签一次都抽不到它**。所以"投放 → 文明一定是外来者"
       这句话，是靠这里保证的，不是靠概率。

       ⚠️ 只强制**物种**。性格和形态照常掷 ——
          他们也住在这颗星球上，也该有形态、也该有自己的脾气。

       ⚠️ 和观测异常的优先级：**投放赢**（上面那个 `&&` 那一行做的）。
          两者都是"强制覆盖物种"，撞上只能有一个赢，
          而要的是"投放之后**必须**是那种生物"。 */
    var forced = world.outsiderSeeded ? forcedProto() : null;
    var picked = forced ? forced.key
                        : RNG.weighted(rng, weightsFor(world, temper));
    var def = byKey(picked);

    // ── ④ 最后掷形态 ──
    var form = drawOne(rng, FORMS, world);

    // ⚠️ 颜色直接抄到 world.civ 上 —— 渲染层靠它决定文明光点画成什么色。
    //    这样渲染层就不用依赖 civ.js，只认"文明档案"这个数据结构。
    world.civ = {
      proto: picked, protoName: def.name, color: def.color,
      /* ★ 族名（兽群 / 巢群 / 潮民 / 林体 / 岩体 / 意群 / 人族）——
         编年史里当主语用。**通用事件也靠它**（`{folk}` 占位符）：
         通用事件所有物种共用，写死「他们」的话，
         虫类、植类、石类的编年史里就会冒出「他们」，
         而它们的族名是巢群 / 林体 / 岩体 —— 不是人。
         （同一类问题 2026-09-12 在历史行上犯过一次，见 CLAUDE.md 第十三节
           「通用历史行已经不用了」那段。） */
      folk: def.folk || def.name,
      form: form.key, formName: form.name, formColor: form.color,
      temper: temper.key, temperName: temper.name,
      isEgg: false,
      /* ★ 四属性的**起点**：物种基准 + 性格加成（见 initialAttrs）。
         之后由文明阶段的小事件推拉（`civEvents.js` 的 `OPTION_ATTR`），
         最后进 `ending.js` 的命运判定。
         ⚠️ 只在这一刻算一次 —— 之后世界怎么变都不再回头算它。 */
      attr: initialAttrs(picked, temper.key)
    };

    return world.civ;
  }

  /**
   * 按权重抽一条。
   *
   * ⚠️ 原来这里还有第四个参数 `bias`（外部塞进来的额外权重）——
   *    2026-09-14 删掉了：全项目只有已删除的第三道岔路口传过它。
   *    物种那边的偏向走的是另一条路（`weightsFor` 直接读 `route.protoBias`），
   *    不经过这个函数。
   */
  function drawOne(rng, list, world) {
    var table = {};
    for (var i = 0; i < list.length; i++) {
      table[list[i].key] = list[i].base + list[i].bonus(world);
    }
    var key = RNG.weighted(rng, table);
    for (var j = 0; j < list.length; j++) {
      if (list[j].key === key) return list[j];
    }
    return list[0];
  }

  /** 按 key 在某一组定义里查一条 */
  function findIn(list, key) {
    for (var i = 0; i < list.length; i++) if (list[i].key === key) return list[i];
    return null;
  }

  /* ═══════════════════════════════════════════════════════════════
     工具
     ═══════════════════════════════════════════════════════════════ */

  function byKey(key) {
    for (var i = 0; i < PROTOS.length; i++) {
      if (PROTOS[i].key === key) return PROTOS[i];
    }
    /* ⚠️ 修真彩蛋也要查得到 —— ending.js 的结局那段现在从
       这张表里读 desc（以前是硬写在 ending.js 里的一个字符串，
       收集不到，是文风盲区）。 */
    for (var j = 0; j < ANOMALIES.length; j++) {
      if (ANOMALIES[j].key === key) return ANOMALIES[j];
    }
    return PROTOS[0];
  }

  /**
   * 那个「只能被强制投放的物种」（现在只有外来者一个）。找不到返回 null。
   *
   * ⚠️ 为什么要一个函数、而不是在别处直接写 `'alien'`：
   *    那个 key 只该出现在 PROTOS 那一条里。写第二遍的话，
   *    哪天改了 key，另一处会**静默**找不到 —— 投放发生了，物种却没变。
   */
  function forcedProto() {
    for (var i = 0; i < PROTOS.length; i++) {
      if (PROTOS[i].forced) return PROTOS[i];
    }
    return null;
  }

  /* ⚠️ 2026-09-22 删掉了 `folkOf(key)` —— 真死代码（全项目零调用，含测试）。
     它返回「族名」（兽类 → 兽群），原注释说"编年史一律用它当主语"，
     但实际早就不走它了：**族名是 `PROTOS[].folk` 这个字段**，
     由 `Civ.roll` 写进 `civ.folk`，编年史里走 `{folk}` 占位符、
     由 `CivLore.fillPlaceholders` 替换。

     ⚠️ 但它那条注释里有**一条还成立的规矩**，别跟着丢了 ——
        **两个名字各管一处，别混**：

          族名（`PROTOS[].folk`：兽群 / 潮民 / 林体）
              → 编年史、事件、征兆里当**主语**。
                规范第一条是"主语必须明确"，而族名才是这个族群
                **自己的叫法**；分类名读着像报告。

          分类名（`PROTOS[].name`：兽类 / 海类 / 植类）
              → 诞生面板 / 结局面板的「原型」那一行，那是**分类信息**。 */
  function hasTrait(world, t) {
    return world.traits.indexOf(t) >= 0;
  }

  /**
   * 算一遍所有原型的权重（不掷骰）。
   * 用途：测试里验证"水世界真的更容易出海类"，以及调参时看权重分布。
   */
  function weightsFor(world, temper) {
    var faithful = !!(temper && temper.key === 'faith');
    var bias = world.route && world.route.protoBias;
    var table = {};
    for (var i = 0; i < PROTOS.length; i++) {
      var p = PROTOS[i];
      var w = p.base + p.bonus(world);
      if (faithful && p.key === 'psionic') w *= 2;
      // ★ 玩家的「生态抉择」偏向（见 choices.js）——
      //   选了"动物先出现"，兽类/人类/虫类就大幅靠前，但同样不保证。
      //   ⚠️ 放在 faith 的翻倍**之后**：信灵能的文明连玩家意志也能扭过去一点，
      //      这是有意的 —— 性格的偏执该压得过外来的推动。
      if (bias) w += bias[p.key] || 0;
      table[p.key] = w;
    }
    return table;
  }

  /* ═══════════════════════════════════════════════════════════════
     文明的名字
     ═══════════════════════════════════════════════════════════════
     为什么要给文明起名字：
       一个文明如果只叫「兽类 · 大陆文明 · 求知」，那它是个**分类标签**；
       叫「灰烬联邦」，它才像个**真的文明**。截图发出去也更有故事感。

     ★ 两段拼起来，而且两段都有来由（不是随机凑词）：

         前缀 ← 他们的物种（住在哪、长什么样、吃什么）
         后缀 ← 他们的性格（怎么组织起来）

       海类 → 潮汐   →  潮汐议会
       石类 → 苍岩   →  苍岩铁誓
       人类 → 河谷   →  河谷同盟

     这样名字能反过来印证结局卡片上的信息 ——
     看到「苍岩」，再看「石类 · 山地文明」，会觉得对得上。
     ═══════════════════════════════════════════════════════════════ */

  /* 前缀词库：按物种分组。抽签时只从自己物种那一组里抽。
     ⚠️ 全部避开修仙味道的字（金木水火土、灵、玄、神、仙…），
        见 CLAUDE.md 第十二节。 */
  var NAME_PREFIX = {
    mammalia:   ['灰烬', '长野', '裂蹄', '荒原', '暮色', '石牙'],
    arthropoda: ['虫巢', '密林', '无数', '腐叶', '薄雾', '细沙'],
    mollusca:   ['潮汐', '深湾', '静水', '珊瑚', '暗流', '海沟'],
    plantae:    ['密荫', '藤桥', '年轮', '翠谷', '根网', '落叶'],
    lithoid:    ['苍岩', '深脉', '铁脊', '熔核', '碎石', '岩层'],
    psionic:    ['回响', '幽思', '共振', '虚白', '梦潮', '远声'],
    humanoid:   ['河谷', '篝火', '石垒', '远路', '初耕', '长夏'],
    /* ★ 外来者（第 8 个物种，只能被界外投放带出来）。
       ⚠️ 漏了这行是**静默**的 —— `civName` 会回退成 `NAME_PREFIX.mammalia`，
          于是外来者的文明名会带着兽类的味道（"灰烬议会"），不报错。
       词都挑"来历不明"那一类：落种 / 异壤 / 无源 / 空坠 / 外源 / 借壤。 */
    alien:      ['落种', '异壤', '无源', '空坠', '外源', '借壤'],
    // ⚡ 修真彩蛋专用（1% 的那一个）。名字后面会再缀一个「· 录外」，
    //    "录外"就是"不在记录之内"。
    //    ⚠️ 词挑得比别处**野**一点（云外 / 剑鸣 / 丹霞 / 九霄）——
    //       但**它们碰不到 `_civEvents_test.js` 那张"文明名里不许有修仙词"
    //       的表**（那张表是另一张 10 词表，和 `_style_test.js` 的 XIUXIAN
    //       不是一回事）。动这几个词之前先核那张表。
    cultivation: ['云外', '剑鸣', '丹霞', '九霄']
  };

  /* 后缀词库：按**性格**分组 —— 他们怎么组织起来，就叫什么。
     ⚠️ 也不出现"帝国""王朝"这类太像历史课本的词，
        换成更中性的组织形态（议会 / 联邦 / 共治体…）。 */
  var NAME_SUFFIX = {
    peace:  ['联邦', '共治体', '同盟', '联合体'],
    war:    ['铁誓', '战团', '征伐军', '锐锋'],
    seek:   ['议会', '学会', '观测团', '探索同盟'],
    faith:  ['教团', '誓约', '圣所', '礼拜团'],
    hermit: ['隐修会', '静修院', '闭守者', '内环'],
    nomad:  ['游群', '商队', '流浪团', '迁徙者'],
    trade:  ['商会', '贸易邦', '港口联盟', '集市'],
    craft:  ['工匠联盟', '铸造会', '营造团', '锤炼者']
  };

  /* 文明名的长度上限。
     ⚠️⚠️ **三处必须一致**：这里 / `app.js` 的 `CIV_NAME_MAX` /
        `index.html` 输入框的 `maxlength`。`_wiring_test.js` 有一条断言盯着。
     ⚠️ 它管的是"**推荐名也得是合法名字**"：见下面 `civName` 里那段。 */
  var NAME_LIMIT = 8;

  /**
   * 给这个文明**推荐**一个名字。
   *
   * ⚠️ 结果**完全由世界种子 + 已经掷定的三层决定** ——
   *    同一颗世界重玩一遍，推荐的名字是一样的。
   *    这样名字不会变成"每次刷新都换一个"的噪声。
   *
   * ⚠️ 这个函数现在**只是推荐值**，不是最终名字 ——
   *    2026-09-13 起玩家可以在「文明诞生」面板上自己改（见 displayName）。
   *    界面把它预填进输入框，玩家不改就直接用。
   *
   * @param {object} world 世界档案（需要 world.civ 已经掷过）
   * @param {number} [roll] 第几次【换一个】（不给 = 第一次，结果和以前一模一样）
   * @returns {string} 比如 '潮汐议会'
   */
  function civName(world, roll) {
    var civ = world.civ;
    if (!civ || !civ.proto) return '';

    /* 用种子派生一条独立的随机序列（和 roll 那条错开，互不干扰）。
       ⚠️ 加法写在**异或前面**：roll 缺省时 `seedNum + 0` 就是 seedNum，
          所以 `civName(w, 0)` 和改动之前的 `civName(w)` 结果完全相同 ——
          老世界的名字一个都不会变。 */
    var rng = RNG.makeRng(((world.seedNum + (roll || 0) * 7919) ^ 0x7A11E5) >>> 0);

    var pre = NAME_PREFIX[civ.proto] || NAME_PREFIX.mammalia;
    var suf = NAME_SUFFIX[civ.temper] || NAME_SUFFIX.peace;

    var a = pre[Math.floor(rng() * pre.length)];
    var b = suf[Math.floor(rng() * suf.length)];

    /* ⚡ 修真彩蛋：推荐名里带一个后缀，一眼看出不一样。
       「录外」= 不在记录之内。
       ⚠️ 它只是**推荐值的一部分**：玩家想删就删（已拍板：不强制）。

       ⚠️⚠️ 但**不许把名字顶过上限** ⚠️⚠️
          这个推荐值会被**原样填进** `maxlength` 的输入框，超一个字
          浏览器就**悄悄截掉最后一个字** —— 玩家看到的是
          「剑鸣联合体· 录」这种断尾巴的名字（实测踩到，见 `_xiu-flourish.png`）。
          所以超了就不加后缀：**推荐名自己必须是一个合法名字**。
          ⚡ 记号在输入框**外面**，彩蛋不会因为少了后缀就认不出来。 */
    if (civ.isEgg) {
      var egg = a + b + '· 录外';
      return egg.length > NAME_LIMIT ? (a + b) : egg;
    }

    return a + b;
  }

  /**
   * 这个文明**最终叫什么** —— 界面一律调它，不要直接调 civName。
   *
   * 顺序：玩家起过名就用玩家的（world.civ.title），没起过就现推一个。
   *
   * ⚠️⚠️ 名字出现在**三个地方**：文明诞生面板 / 编年史顶上那行 / 结局卡片。
   *      三处必须都走这个函数 —— 漏一处就会出现"编年史叫 A、结局卡片叫 B"，
   *      而且**不报错**，只有玩家看得出来。
   *
   * ⚠️ 名字**不参与任何生成逻辑**，只是贴在卡片上的标签 ——
   *    和 world.name（世界名）是同一条规矩。
   *    所以它不影响「同种子 = 同世界」：别人拿到你的种子，
   *    复现出的世界和文明一模一样（只是名字不同）。
   *
   * @param {object} world 世界档案
   * @returns {string}
   */
  function displayName(world) {
    var civ = world && world.civ;
    if (!civ || !civ.proto) return '';
    return civ.title || civName(world);
  }

  return {
    PROTOS: PROTOS,
    FORMS: FORMS,
    TEMPERS: TEMPERS,
    ANOMALIES: ANOMALIES,
    ANOMALY_CHANCE: ANOMALY_CHANCE,
    NAME_LIMIT: NAME_LIMIT,
    NAME_PREFIX: NAME_PREFIX,
    NAME_SUFFIX: NAME_SUFFIX,

    /* ★ 三行信息的**标签**（2026-09-16 加）。
       ⚠️⚠️ 抽成常量是因为它**同一个词要印在四个地方** ——
          「文明诞生」面板 / 结局卡 / 记录页 / 三个预览工具。
          手抄的话，哪天改一处漏一处，界面上就会出现
          "诞生面板叫 A、结局卡叫 B"，**而且不报错**（本项目栽过好几次）。

       ⚠️ `temper` 那一行原来叫「**伦理**」—— 那是早期一套
          **从来没被写入过**的字段（`world.civ.ethics`）留下的名字。
          数据早就是性格了（和平 / 征战 / 求知…），标签一直没跟着换。
          2026-09-16 拍板改成「**倾向**」：它是中性的**观察词**，
          配"科学考察报告"的调子（「性格」「性情」都太人味）。

       ⚠️ 修真彩蛋的 `temper` 是 null，那一行整个不出现 ——
          所以这三个标签不必为它特判。 */
    ROW_LABELS: { proto: '原型', form: '特质', temper: '倾向' },

    /* ★ 四属性（2026-09-15）。
       ⚠️ `ATTR_SPECIES` / `ATTR_TEMPER` 也导出去了 —— `_wiring_test.js`
          要拿 `PROTOS` 逐个对拍，确认每个物种都有一行（漏了就是静默的 0）。 */
    ATTR_KEYS: ATTR_KEYS,
    ATTR_NAMES: ATTR_NAMES,
    ATTR_SPECIES: ATTR_SPECIES,
    ATTR_TEMPER: ATTR_TEMPER,
    ATTR_RULES: ATTR_RULES,
    initialAttrs: initialAttrs,
    attrOf: attrOf,
    reliefFor: reliefFor,

    roll: roll,
    civName: civName,
    displayName: displayName,
    byKey: byKey,
    forcedProto: forcedProto,
    /* ⚠️ 2026-09-22：`folkOf` 导出删了（真死代码，见上面那段墓碑）。 */
    formOf: function (key) { return findIn(FORMS, key); },
    temperOf: function (key) { return findIn(TEMPERS, key); },
    weightsFor: weightsFor
  };
});
