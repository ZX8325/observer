/* ═══════════════════════════════════════════════════════════════════
   ⑧ Ending —— 结局：评定 + 分支 + 传记
   ═══════════════════════════════════════════════════════════════════
   职责：世界走到文明之后，根据它的最终状态给出一份"结案报告"。

   三样东西：
     ① 世界评定 —— 看四个本源的综合分（荒芜 → 完美世界）
     ② 结局分支 —— 看有没有触发特殊条件（寂灭 / 破碎 / 极端 …）
     ③ 世界传记 —— 一段 50~100 字的文字，像自然史观察日志

   ⚠️ 文风要求（见 CLAUDE.md 第十二节）：
      像地质学家写的观察日志，不像小说。
      常规世界里**不许出现**"灵气""渡劫""境界""宗门"这类词。
      唯一的例外是那 1% 的修真彩蛋 —— 而且口吻必须是**观察者的困惑**，
      不是小说家的热血。

   铁律 B：本文件不许出现 document / window / canvas。
   ═══════════════════════════════════════════════════════════════════ */

(function (root, factory) {
  var mod = factory(
    (typeof require !== 'undefined') ? require('./civ.js') : root.Civ,
    (typeof require !== 'undefined') ? require('./civEvents.js') : root.CivEvents,
    /* ⚠️ 2026-09-12 加：传记收尾要写"共计多少个周期"，
       而周期数只能由 CivLore.cyclesOf 现算（事件里的 slow 会改编年史时长）。
       **不要在本文件里手抄一份 CYCLE_PER_SEC** —— 那正是 PACE 踩过的坑
       （见 CLAUDE.md「全局节奏 PACE」：手抄的副本一定会忘记同步）。 */
    (typeof require !== 'undefined') ? require('./civLore.js') : root.CivLore
  );
  if (typeof module !== 'undefined' && module.exports) module.exports = mod;
  else root.Ending = mod;
})(typeof globalThis !== 'undefined' ? globalThis : this,
   function (Civ, CivEvents, CivLore) {
  'use strict';

  /* ═══════════════════════════════════════════════════════════════
     ① 世界评定 —— 看四个本源
     ═══════════════════════════════════════════════════════════════ */
  function rate(world) {
    var e = world.essence;
    var keys = ['energy', 'water', 'atmo', 'land'];
    var i, sum = 0, min = 100;

    for (i = 0; i < keys.length; i++) {
      var v = e[keys[i]];
      sum += v;
      if (v < min) min = v;
    }
    var avg = sum / keys.length;

    // 任意一项太低 → 直接判荒芜（一颗星球只要缺一样致命的，整体就不成立）
    if (avg < 25 || min < 10) return { key: 'barren', name: '荒芜' };
    if (avg < 45)             return { key: 'poor',   name: '贫瘠' };

    /* ★ 此界不成（卡住没走完）的世界，评定不能高于「贫瘠」。
       ⚠️ 不加这条会写出自相矛盾的卡片：
          传记说"热量几近散尽"，评定却写"宜居" ——
          因为 rate() 只是四项求平均，水多陆多就把能量归零这点盖过去了。
        "宜居"的判据本来就该是"能不能住"，而不只是分数平均。 */
    if (world.stalled)        return { key: 'poor',   name: '贫瘠' };

    if (avg < 65)             return { key: 'humble', name: '宜居' };

    // 完美世界：不仅平均分高，而且**四项都不能拖后腿** ——
    // 平均分高但有一项是 20 分，那不算完美。
    if (avg > 82 && min >= 60) return { key: 'perfect', name: '完美世界' };

    // 走到这里说明 avg >= 65 且四项都不低于 10 —— 就是丰饶
    return { key: 'rich', name: '丰饶' };
  }

  /* ═══════════════════════════════════════════════════════════════
     ② 文明命运 —— 「这世界最后怎么了」
     ═══════════════════════════════════════════════════════════════

     按优先级从上往下判断，命中就停。
     ⚠️ 顺序就是设计：越靠前越罕见、越有戏剧性。

     ⚠️ 「被观测」放在中间是故意的：
        它需要玩家几乎没干预过。如果放在最后，那些本来就满足
        其它条件的世界会先把它抢走。
     ═══════════════════════════════════════════════════════════════ */
  var FATES = {
    stalled: { key: 'stalled', name: '未成形',
               desc: '此界始终没能跨过那道门槛。结构长期停滞，此后没有再出现新的变化' },
    // ★ 2026-09-12 重做文明阶段时加的：编年史期间压力涨满 100
    // ⚠️ 这条的 desc 必须留着「压力」二字 —— _civEvents_test.js 拿
    //    /溃散|压力|散掉/ 认溃散结局，改没了那条断言会红。
    collapse:{ key: 'collapse', name: '溃散',
               desc: '压力超过了文明能承受的限度。它在自己的重量下散掉了' },
    rift:    { key: 'rift',    name: '世界破碎',
               desc: '世界内部的应力突破临界。地表出现贯通性裂缝，观测随之中断' },

    /* ★★ 2026-09-14：收割 —— 界外之物那条线的批 2 ★★

       来历：界外之物在文明阶段回访过一次（只有被它们投放过的世界才有，
       见 civEvents.js 的 VISIT），而观察者**选择了不出手**、
       此界又没能自己扛过去（3%）。那之后就没有然后了。

       ── 为什么它是一个**世界级布尔**（`world.invaded`），不是 `marks` ──
          `marks` 是"玩家在文明阶段一次次选出来的倾向"，
          处在 fate 链的**倒数第二档** —— 它说的是"文明变成了什么样"。
          而这一条和 `world.fractured`（世界破碎）**完全同构**：
          一锤定音、压过其余一切。所以它排在同一个位置（紧跟 fractured）。
          ⚠️ 塞进 marks 的话，它会被光暗值那几档抢走 ——
             一个被从外面取走的世界，不该因为玩家投过光种就报「飞升」。

       ⚠️ desc 要能**单独读**（它在结局面板的「命运」那一行直接显示），
          所以不靠前文也能成立。 */
    /* ⚠️ 2026-09-15 用户改的（第二版）。原来写的是：
         『有东西从记录之外进来，取走了此界长出来的东西。观测到此为止，没有新的条目』
       改成了**画面**而不是动作 —— 「取走了东西」是概括，
       「聚居点一个接一个停止运转，用了不到三个周期」才是看得见的事。
       和文风规范那条（不写摘要写画面）是一致的。

       ⚠️ 这一条的 desc **不走通用模板** —— `bio()` 里收割走的是
          **专属分支**（见下面那段）。
       ★ 2026-09-17：通用模板那句前缀 `'此后，'` 已删（见 bio 里那条），
          所以"desc 里不许有此后"这条老约束**整个不成立了**。
          这里 desc 里的「此后，」现在是正文的一部分，照读。 */
    harvest: { key: 'harvest', name: '收割',
               desc: '有什么从记录之外进来了。此后，聚居点一个接一个停止运转。从第一个到最后一个，用了不到三个周期。观测到此为止，没有新的条目' },
    doom:    { key: 'doom',    name: '寂灭',
               desc: '文明在灾难中消失，只剩散落的遗迹。遗迹存续的时间比文明本身长' },
    /* ⚠️⚠️ 2026-09-22 重写 desc：**它才是"发现造物主"那条，却一个字没提观测者** ⚠️⚠️

       ── 原来为什么会读成外星 ──
       原文：「文明察觉到**世界之外另有存在**，向那个方向反复发出信号。
              信号至今没有得到回应」

       「世界之外另有存在」这一句**本身就长着外星人的样子** ——
       而全条**没有一处提到观测者**，玩家只能读成"发现了别的文明"。

       ── 但这条真正的意思就是"发现了你" ──
         · **预兆画面**（`evolution.js` 的 `OMENS.watched`）写的是：
              「所有光点同时转向，**朝向观测者**」  ← ★ 决定性
         · 代码注释（本文件 191 行）：「被观测 = 他们朝外面发信号，**没人理**」
         · ⑧`seent` 那一屏就是"他们发现了造物主"（用户 2026-09-22 定的）

       ── 改法 ──
         · 前一句 → 「察觉到**有什么在看着他们**」：这才是他们真正的体验
           （也是「被观测」这个**名字**的字面意思）。
           ⚠️ 措辞和 ⑧ 的 question「他们认定那个方向有东西，
              **而且一直在看着他们**」对齐 —— 同一件事，两屏用同一句话。
         · 后一句 → 点名「**观测者**」，和「抵达」成对：
              抵达   = …回应来自**观测者**
              被观测 = **观测者**至今没有回应   ← 只差"有没有回"
           ⚠️ 这一对必须两边都点名，玩家才看得出它们是同一件事的两个结局。

       ⚠️ 「世界之外」这个说法**在别处是对的、在这里是错的**：
          收割（本文件 122 行）的「有什么从记录之外进来了」讲的是**界外之物**，
          那个"之外"就该是模糊的（观测者也不知道那是什么）—— 一个字没动。 */
    watched: { key: 'watched', name: '被观测',
               desc: '文明察觉到有什么在看着他们，向那个方向反复发出信号。观测者至今没有回应' },

    /* ═══════════════════════════════════════════════════════════════
       ★★ 信仰专属结局「仰止」（2026-09-22 用户拍板）★★
       ═══════════════════════════════════════════════════════════════

       ── 唯一的入口（用户原话：「信仰触发了事件后如果观测者选择回应，
            直接会进入专属结局」）──
           性格 = **信仰**  **且**  ⑧`seent` 的【现身】（`contact` 攒够）
         ⚠️ **不算概率、不做第二道筛选** —— 满足这两条就直接进。

       ── 为什么必须是两个条件一起 ──
         · 别的性格现身 → 走「抵达」（他们找到了你，仅此而已）
         · 信仰但你没现身 → 走「被观测」（他们信，但你没回应）
         只有"**本来就在信**"碰上"**你真的出现了**"，才会长出这个东西。

       ── 名字 ──
         「仰止」出自「高山仰止，景行行止」。
         ⚠️ 那个**「止」字是这条结局的意思**：
            仰望变成了制度，`抬头看`这件事本身停了。
         ⚠️ 它同时是**删掉的 ①`lookup`「{folk}开始仰望」的收口** ——
            那个「仰」和这个「仰」是同一个字，一条线从"第一次抬头"
            走到"再没有人抬头"。

       ── 用户定的内容（三条，desc 写不下，第二条在 `bio()` 里）──
         ① 观测者被当成神崇拜，人民的信仰统一了，但这个信仰**也被宗教扭曲了**
         ② ★ **这不是个好结局，所以需要制造麻木感**
            （desc 里那句「再没有人问过为什么」就是它）

       ⚠️ 触发的那两条券**都不动**：⑧【现身】照旧给 `contact: 2`，
          信仰照旧是那个性格。这一条只是**在判定链上加一个出口**。 */
    revere:  { key: 'revere',   name: '仰止',
               desc: '文明把你当成了神。信仰就此统一，别的解释都被取消。再没有人问过为什么' },
    ascend:  { key: 'ascend',  name: '飞升',
               desc: '文明突破了此界的限制，离开这颗星球。去向没有记录' },
    split:   { key: 'split',   name: '分裂',
               desc: '文明内部裂开，分成互不往来的势力。两边的记录，开头完全相同' },
    merge:   { key: 'merge',   name: '融合',
               desc: '原本不同的族群最终合成一体，不再分彼此。区分他们的那套说法被废除了' },
    cycle:   { key: 'cycle',   name: '轮回',
               desc: '文明几经兴衰，又回到起点。此前每一次的记录都没有传下来' },

    /* ★ 2026-09-13 加的第十条命运 —— 「机械」这个物种改成的结局
       ─────────────────────────────────────────────────────────
       用户的原话：「如果把机械变成结局呢」

       ── 为什么机械类当初必须从物种里删掉 ──
       别的物种都在回答「**什么样的世界长出它**」：
       水多的世界长出潮民、石头多的长出岩体。
       而机械是**造出来的，不是长出来的** —— 它答不上这道题。
       它是唯一一个**违反游戏自己因果律**的物种。

       ── 改成结局之后就成立了 ──
       因为它不再是"某个世界长出来的东西"，而变成了
       **"某个文明做出来的事"** —— 而"文明会造出替自己干活的东西"
       是这个世界观里完全说得通的一段历史。

       ⚠️ desc 会被结局传记直接引用（★ 2026-09-17 起是 `f.desc + '。'`，
          前缀「此后，」已删），所以：**不能自带句号**，
          而且要以「文明 / 他们」当主语 ——
          前面那句是「文明在其上崛起：…」，主语接得上。 */
    /* ⚠️⚠️ 这一句改过**两次**，两次都是用户点的 ⚠️⚠️

       ── 第一次（2026-09-14）：玩家看不懂 ──
       原来：『文明把能做的事一件件交了出去，最后交出了自己。留下的那些还在运转』
         ↑ "交出了自己"是**最含糊的一句**（是死了？走了？变成机器了？）
         ↑ "留下的那些"也没说是人还是东西
       改成把**后半句挑明**：东西还在动，而人不见了。

       ── 第二次（2026-09-22）：和「同行」配成一对 ──
       用户给了新的一句，**开头和「同行」一字不差**：

           遗落：他们造出的东西有一天说了「我」。许多年后，那些东西还在运转，**他们不见了**
           同行：他们造出的东西有一天说了「我」，冲突持续了几代人，此后，
                 两边渐渐理解了彼此，**文明走向繁荣**

       ⚠️⚠️ **两条共用第一句，只在后面分岔** —— 这是刻意的，别把哪一条的开头
          改回去：玩家两条都见过的话，一眼就知道是一件事的两个下场。
          `_civEvents_test.js` 里有一条断言钉着"两边都跑得通"。
       ⚠️ 分岔的标点也不同：遗落那句「说了「我」」后面是**句号**（那件事就此结束），
          同行后面是**逗号**（那件事只是一连串事情的开头）。
       ⚠️ 「不再动手」那半句去掉了 —— 用户改写时不要了。意思没丢：
          「他们不见了」比"不动手"重得多。
       ⚠️ 和上面那条老规矩不冲突：主语仍是「他们」，接得上前一句
          「文明在其上崛起：…」；也仍然**不自带句号**（传记会补一个）。 */
    machine: { key: 'machine', name: '遗落',
               desc: '他们造出的东西有一天说了「我」。许多年后，那些东西还在运转，他们不见了' },
    /* ═══ ★★ 同行 —— 「遗落」的**另一条岔路**（2026-09-22 用户拍板）★★ ═══

       ── 它和遗落是同一个 mark 的两半 ──
       两个结局的入口**完全一样**（生产最高 + `machine` 攒够），
       只在最后按**文化**分岔：

           文化 < 2.5  → 遗落   （人不见了，东西还在转）
           文化 ≥ 2.5  → 同行   （打过一场，最后两边站到了一块）

       ── ⚠️ 为什么界是 2.5，不是整数 ⚠️ ──
       界面上的文化是**四舍五入过的整数**（`app.js` 的 `Math.round`），
       而内部是小数。`Math.round` 是**四舍五入**的 —— 真值 **2.5 显示成 3**。
       ⇒ 这个界翻译成玩家看得见的话就是：

             文化显示 2 及以下 → 遗落
             文化显示 3 及以上 → 同行

       ⚠️ 反过来，整数界（2 或 3）**对不上界面**：界写 3 的话，
          真值 2.9 明明显示着「3」，却会走遗落。**2.5 是唯一吻合的那个。**
       ⚠️ 实测（会报遗落的 267 局）文化中位是 **2.55**，
          所以 2.5 也正好把这一批**对半切开**（47% / 53%）。

       ── 为什么读 `Civ.attrOf` 而不是 `attrCapped` ──
       界面显示走的就是 `attrOf`。`attrCapped` 只在超过上限 10 时才夹，
       而这批局文化最高才 5.8 —— 两个函数在这里结果一样，
       但**该跟显示用同一个入口**。

       ── ⚠️ 名字改过一次：「共存」→「同行」（2026-09-22 用户拍板）⚠️ ──
       「共生」（`symbiose`）**是另一条结局**，早就在了 —— 它在「文化最高」
       那一支里，说的是「文明长进生态里」。这一条说的是「文明和自己造的东西
       并肩」，用户定的名字**原来叫「共存」**。
       ⚠️ 但「共存」和「共生」只差一个字，两条在结局列表里还挨着 ——
          玩家一眼扫过去会看混。用户改定为**「同行」**（走在一条路上）。
       ⚠️ 只动**显示名**，key 仍是 `coexist`（红线①：只改字符串，不改 key）
          —— `world.lockedFate`、`OMENS`、`renderer` 认的都是这个 key。
       ⚠️ 老名字那一对**"结局名不许互相包含"抓不到**（共存 和 共生 谁也不包含
          谁，只是长得像）；换成「同行」之后连"长得像"也没了。 */
    coexist: { key: 'coexist', name: '同行',
               /* ⚠️ 长度量出来的：**44 字**，上限 45。
                  `_style_test.js` 那条按 `。！？` 切句 —— **逗号不算断句**，
                  所以这一整句算一句，44 字压线过。

                  ⚠️⚠️ 引号：用户原话里写的是**弯引号**，这里换成了直角引号 ——
                      全项目弯引号出现 **0 次**，给玩家看的文案里一律是直角引号
                      （见 `civEvents.js` 的「接下来该怎么走…」那一句）。
                  ⚠️ 2026-09-22 之前这一句是「他们造出的东西有一天做了件
                      没人吩咐它做的事…」，用户改成了「**说了「我」**」——
                      机器的第一次自称，比"做了件事"具体得多。
                  ⚠️⚠️ 同一轮里**「遗落」的开头也改成了这一句** ——
                      两条现在共用第一句，只在后面分岔：

                          遗落：…说了「我」**。**许多年后…他们不见了
                          同行：…说了「我」**，**冲突持续了几代人…文明走向繁荣

                      分岔的不只是内容，**标点也不同**：遗落是句号（那件事到此为止），
                      同行是逗号（那件事只是一连串事情的开头）。 */
               desc: '他们造出的东西有一天说了「我」，冲突持续了几代人，此后，两边渐渐理解了彼此，文明走向繁荣' },

    /* ═══════════════════════════════════════════════════════════
       ★★ 2026-09-13：命运从 10 条扩到 17 条 ══

       起因：实测分布发现**「溃散」一个人占了 52%** ——
       玩家每玩两局就有一局是同一个结局。所以这次扩展分两路走：

         A 组（3 条）把「溃散」按**性格**拆开 —— 同样是压力满 100，
                    征战型和隐世型不该崩成一个样子。
         B 组（4 条）给**走完了**的文明补几个新收场。

       ── A 组：崩法的三种变体 ──
       ★ 这不是"新的死法"，是**同一个死法的不同形状**。
         它们共用 collapse 的传记骨架（都引用编年史最后一行），
         只有"怎么没的"那一句不同 —— 见 bio() 里那一段。

       ⚠️ 分派读的是 `world.civ.temper`（性格 key）。它**一定存在** ——
          进文明阶段时先掷性格再抽物种（见 CLAUDE.md 第七节 3.3）。
          取不到就退回 collapse，不会漏。 */
    blaze:   { key: 'blaze',    name: '燎原',
               desc: '冲突持续到没有任何一方还在记录。中断发生在同一个周期' },
    recede:  { key: 'recede',   name: '退潮',
               desc: '他们退回自己划定的边界以内，不再向外。边界以外的记录没有新的条目' },
    disperse:{ key: 'disperse', name: '星散',
               desc: '维系彼此之间的往来先断了。各处的记录互不相干' },

    /* ── B 组：走完了的新收场 ──

       ★ 「抵达」和「被观测」是**一对**，判定条件只差一个 `dropTally.light`：
           被观测 = 他们朝外面发信号，**没人理**
           抵达   = 同样发了信号，**你回了**（投过光种 = 降下启示）
         两张截图摆在一起，故事才完整。

       ★ 「静滞」是这批里最冷的一条 ——
         比"他们死了"更冷的是"他们不再变化了"。
         它从 cycle（轮回）那一堆里分出来：撑到最后 + 压力从没上过档。

       ★ 「共生」把"文明"和"生态"合成一件事 ——
         现有的 merge 说的是两个**族群**合成一个，不是这个。

       ★ 「培育」是**投放多**的奖励，和"被观测"（投放少）正好对称：
         你插手插得越深，这颗星球就越像他们（也就是你）要的样子。 */
    /* ⚠️⚠️ 2026-09-22 改 desc：**「记录之外的某处」这个词是两可的** ⚠️⚠️

       ── 原来为什么会读错 ──
       用户把它读成了「**到了外星**」—— 而且这个读法**站得住**：
         · 观测者**在**记录之外（它不属于这个世界，它是写记录的那个）
         · 界外之物**也在**记录之外（⑪ 的 question 原文就是
           「有东西从**记录之外**进来」）
       同一个词，两条线都在用，所以「回应来自记录之外的某处」
       既可以读成"你回了"，也可以读成"另一个外星文明回了"。

       ── 改法 ──
       直接点名 **「观测者」** —— 这个词**只有一个意思**，不能两读。
       ⚠️ 而且它和游戏名（`index.html` 的 `<title>`）以及启动页的
          「欢迎回来，观测者。」是同一个词，玩家见过。

       ★ 这一条和「被观测」是**一对**（见 191 行那段注释）：
           抵达   = 你回了
           被观测 = 没人理
         现在「抵达」点名了是谁回应的，那一对就读得出来了。

       ⚠️ 名字「抵达」**暂时没改**（用户 2026-09-22 定的）——
          它暗示"到了某处"，和 desc 说的"有回应"仍有轻微张力。
          但 desc 点名之后，玩家不会读歪。 */
    arrive:  { key: 'arrive',   name: '抵达',
               desc: '文明向那个方向反复发出信号。信号得到了回应，回应来自观测者' },
    /* ⚠️ 这条的 desc 现在（2026-09-17 起）直接拼在「文明在其上崛起：」后面，
       不带任何前缀。下面记的是它**当初为什么长成这个样子** ——
       那个具体约束（不许出现两个「此后」）已经随前缀一起作废了，
       但选词的道理没变，留着当参考。

       ── 由来 ──
       传记原来是 `'此后，' + f.desc + '。'` 拼的（见 bio 里那条），
       所以 desc 里不能再来一个「此后」，否则拼成
       「此后，文明在某个水平上停住了。此后再没有新的条目」——
       两个「此后」挨着，读起来像结巴（第一版四条全踩了）。

       当时的改法是**去掉时间词**，让"不再 / 一直 / 无法"这些词自己承担
       时间延续的意思 —— 比换个同义词（"从此"）干净。 */
    stillness:{ key: 'stillness',name: '静滞',
               desc: '文明在某个水平上停住了。再没有新的条目，记录一直写到观测结束' },
    symbiose:{ key: 'symbiose', name: '共生',
               desc: '文明成了生态的一部分，不再从里面取用。两者的记录无法分开' },
    cultivate:{ key: 'cultivate',name: '培育',
               desc: '文明开始按自己的需要改变这颗星球。大气与温度不再由自然决定' },

    /* ═══ ★★ 修真彩蛋的两个专属结局（2026-09-16）★★ ═══

       ⚠️⚠️ 这两条**别的世界永远拿不到** ⚠️⚠️
          唯一能把世界送到这里的，是 `fate()` 最前面那句
          `if (civ && civ.isEgg)` 的提前 return。全项目的读者
          （`Ending.evaluate` / `OMENS[...]` / 界面）都只从
          `fate()` 的返回值取，所以那一句就是全部保证。

       ⚠️ 它们是"生 / 死"两条，而且**只有这两条**（用户拍板）：
          这条线没有飞升、没有分裂、没有被观测 —— 撑住了就是长存，
          没撑住就是劫灭，中间没有别的收场。
       ⚠️ desc 里**不写「此后」**（传记那两段是单独写的，
          但通用模板那条"desc 里不许有此后"的规矩在这里也照样遵守）。 */
    xiuDoom: { key: 'xiuDoom', name: '劫灭',
               desc: '最后落下来的那一下，他们没有接住。落点周围的地表被削去一层，' +
                     '只剩下石头' },
    xiuFlourish: { key: 'xiuFlourish', name: '长存',
                   desc: '最难的那几十年过去了，他们一个都没有少。' +
                         '往后的记录里，人数没有再减少过' }
  };

  /**
   * 事件链把结局**钉死**了吗？钉死了返回那个 fate 的 key，否则 null。
   *
   * ★ 2026-09-22 加，给 `evolution.js` 用：一旦钉死就**当场收尾** ——
   *   不再往下排历史行、也不再放大事件，预兆只放 `LOCK_OMEN_SEC` 秒。
   *   用户的原话：「本来锁结局了，就不应该再进别的结局了」。
   *
   * ── 哪几条算"钉死" ──
   *   ① `world.lockedFate`（③【合上这一页】/ ⑦【抹除文明】）—— 玩家亲手点的
   *   ② `marks.fall` 攒够 → 寂灭（`MARK_MIN.fall = 3`）
   *      ⚠️ ⑦【任其消失】走的就是这条，`fall: +3` 一手给满
   *   ③ 信仰 + `marks.contact` 攒够 → 仰止
   *
   * ── ⚠️⚠️ 三条规矩，缺一条就会出"两把尺子" ──
   *   ① **世界级硬标记不算**：`fractured` / `invaded` 一进来就返回 null。
   *      它们不是"事件链锁的"，而且各自有各自的处置
   *      （收割当场收尾、破碎在混沌期就定了）。
   *   ② **顺序必须和 `fate()` 一致**：显式锁 → 寂灭 → 仰止。
   *      ⚠️ `fate()` 里还夹着一档「光暗极端」（排在寂灭**之后**、仰止**之前**）——
   *         这里**不判它**，交给末尾那道自检去拦（见 ③）。
   *      ⚠️ 这个次序 2026-09-22 晚第一次写反过（把光暗极端写到了寂灭前面），
   *         不影响行为（自检兜着），但会带偏下一个读的人。**改之前先对一遍 `fate()`。**
   *      写反了不会报错，只会"这边说锁了、那边报别的"。
   *   ③ **末尾自检**：真拿 `fate()` 算一遍，对不上就**不锁**。
   *      宁可让世界继续跑（最坏也就是晚一点收尾），也不能锁错。
   *      ⚠️ 自检兜的是"以后有人加了新档位、忘了同步这里"——
   *         本项目在"两把尺子量同一件事"上栽过好几次。
   */
  function lockedFate(world) {
    if (!world || !world.civ) return null;
    if (world.fractured || world.invaded) return null;

    var c = world.civ, k = null;

    if (world.lockedFate && FATES[world.lockedFate]) {
      k = world.lockedFate;                       // 显式锁：压倒一切
    } else if (CivEvents.markReached(world, 'fall')) {
      k = 'doom';
    } else if (c.temper === 'faith' && CivEvents.markReached(world, 'contact')) {
      k = 'revere';
    }

    if (!k) return null;

    /* ③ **自检**：真拿 `fate()` 算一遍，对不上就**不锁**。

       ⚠️ 它一个人兜住了两件事，所以别在它前面再写第二份判断：
         · **光暗极端能盖过后两条**（`fate()` 第 3 档排在寂灭/仰止前面）——
           那种局面自检直接判"不锁"，世界继续跑。
         · 以后有人往 `fate()` 里加了新档位、忘了同步这里 ——
           也由它兜住，最坏是晚一点收尾，不会锁错。

       ⚠️⚠️ 原来这里**单写了一条光暗守卫**，负向测试发现**它是死代码**：
           摘掉之后行为一模一样，因为自检本来就会判成不锁。
           按"不留摆设"的规矩删了 —— 和当年删掉的 `evo.outsiderRolled`
           是同一类东西（见 `evolution.js` 那段注释）。 */
    return (fate(world, true).key === k) ? k : null;
  }

  /**
   * 判定这颗世界的命运。
   * @param {object} world
   * @param {boolean} reachedCiv 有没有真的走到文明阶段
   */
  function fate(world, reachedCiv) {
    var e = world.essence;
    var L = world.evo.layers;
    var civ = world.civ;
    var temper = (civ && civ.temper) ? Civ.temperOf(civ.temper) : null;
    /* ⚠️ 性格的 **key**（上面那个 `temper` 是对象）——
       「溃散按性格分派」和「培育认工匠型」都要读它。
       ⚠️ 必须放在**函数顶部**：写进某个分支里的话，没走那个分支时
          读到的是 undefined（var 只提升声明、不提升赋值），
          于是静默走到兜底分支 —— 不报错，只是结局悄悄变了。 */
    var tk = civ && civ.temper;

    /* ═══ 0. ★★ 修真彩蛋：**只有两个结局**（2026-09-16）★★ ═══

       用户的原话：「两个结局分别为压力爆了就给修真界毁灭结局，
       如果存活就给繁盛结局」「修仙世界只会出现这两个结局」。

       ⚠️⚠️ 这是**整个函数的第一句** —— 排在 `fractured` / `invaded`
          前面，不是随手放的 ⚠️⚠️
          那两条是"一锤定音、压过其余一切"的先例，而这条线**没有别的出口**：
          世界碎了也好、被取走也好，它只报"接住了"或"没接住"。
          （那两件事在彩蛋上其实**构造上不可能发生**：`invaded` 要求
            `outsiderSeeded`，而彩蛋骰要求 `!outsiderSeeded`；
            `fractured` 是混沌期投放光暗种的事。所以提前 return
            不会遮住任何**真实发生过**的事 —— 它遮的是"理论上可能"。）

       ⚠️ 判据是 `isEgg`（全项目八处都用它：civPhrase / civName /
          evaluate / 界面那四处…），**不是 `proto === 'cultivation'`** ——
          两者今天等价，但前者在"以后再加一种形态"时仍然对。

       ⚠️ 读 `world.civCollapsed`（**世界级布尔**，和 `harvest` 同构）：
          被压垮那一刻 `stepCiv` 已经把它写上了，读多少次都是同一个答案。
          所以「压力爆了 → 劫灭 / 撑到最后 → 长存」在这里就是一句话。 */
    if (civ && civ.isEgg) {
      return world.civCollapsed ? FATES.xiuDoom : FATES.xiuFlourish;
    }

    // ── 1. 世界破碎（混沌裂隙撕出来的，优先级最高）──
    if (world.fractured) return FATES.rift;

    /* ── 1.5 ★ 收割（界外之物入侵，而观察者没有出手）──
       2026-09-14 加，界外之物那条线的批 2。

       ⚠️ 和「世界破碎」并排放在**最前面**，理由是同一条：
          这两件事都不是"这个文明后来怎么样了"，而是
          **"这颗世界本身出了事"** —— 一锤定音，压过其余一切。
          排在溃散 / 寂灭 / 飞升前面，意味着一个被从外面取走的世界
          不会因为"光暗值刚好够飞升"就报「飞升」。

       ⚠️ 读的是 `world.invaded` 这个**世界级布尔**，不是 `marks`
          （理由见上面 FATES.harvest 那段）。

       ⚠️⚠️ 它必须**排在此界不成（stalled）之前** ⚠️⚠️
          不是因为会撞（要在文明阶段才可能被入侵，而 stalled 的世界
          压根没走到文明），而是因为**读起来的顺序**：
          这里两条都是"世界级的事"，分开摆会让下一个人以为有讲究。 */
    if (world.invaded) return FATES.harvest;

    /* ── 1.7 ★★ 观察者把结局一手定死（9-21 静滞 / 9-22 轮回）★★

       `world.lockedFate` 存的是**一个 FATES 的 key**，由选项自带的
       `lockFate` 字段写进来（见 `civEvents.js` 的 `apply`）。
       现在两个用户：③【合上这一页】→ stillness、⑦【抹除文明】→ cycle。

       ⚠️ 2026-09-22 之前这里是 `if (world.halted) return FATES.stillness;`
          —— 布尔戳只锁得死一种结局，加第二种就得再挂一个同形状的字段。
          改成"存 key"之后两个用户共用这一条判定。

       ⚠️ `FATES[lf]` 那一下判断**不能省**：key 拼错时 `FATES[lf]` 是
          undefined，直接 return 会把整局报成"没有结局"。
          有它则静默落回四属性层 —— 也不理想，但不会崩；
          真正兜底的是 `_civEvents_test.js` 里那条"必须是真 FATES key"。

       ⚠️⚠️⚠️ 2026-09-22 晚：**位置从"仰止之后"提到了这里** ⚠️⚠️⚠️

          起因是用户报的现象：「选了锁结局的选项之后，又跳了几个大事件，
          过了一会才跳结局」，以及他定调的那句话：
          **「本来锁结局了，就不应该再进别的结局了」**。

          旧位置在死活档**之后**（溃散 / 寂灭 / 光暗极端全排在它前面），
          理由是"一个被压垮的世界报静滞是报错了"。**那条理由现在不成立**：
          "锁"只可能由**玩家亲手点的那一下**写进来 ——
          它不是"世界后来出了什么事"，是**玩家的判决**，
          压过世界后来发生的一切。

          ⚠️ 仍然留在它前面的只有 `fractured` 和 `invaded` 两条：
              那是"这颗世界本身没了"，而且都发生在玩家点那一手**之前**。

          ⚠️ 一个具体的差别：③【合上这一页】自己带 `fall: +2`。
              玩家手上要是已经有 1 点 `fall`，点完就够 3 点 ——
              旧顺序下这一手会报成**寂灭**，和选项上写的「合上这一页」对不上。
              新顺序报静滞。

       ⚠️ 配套改动：`evolution.js` 里现在有一条「钉死了就当场收尾」
          （读 `Ending.lockedFate(world)`，预兆只放 `LOCK_OMEN_SEC` 秒）——
          结局不必等编年史写完。两者必须**同向**：这里认什么，那边就锁什么。 */
    var lf = world.lockedFate;
    if (lf && FATES[lf]) return FATES[lf];

    /* ── 2. 此界不成（连门槛都没跨过去）──
       卡在某个阶段过不去、等了 30 秒也没缓过来（见 evolution.js 的 handleStuck）。

       ⚠️ 排在碎裂**之后**、其它一切**之前**：
          一颗连海洋都没凝出来的世界，不该走"文明崛起之后如何如何"的分支。
          但真被撕碎过的世界仍然优先报「世界破碎」—— 那更戏剧性。

       ⚠️ 这一条让"卡住"从**没有结局**变成了**一种结局**。
          改之前 11.5% 的世界会永远停住，玩家只能重开。 */
    if (world.stalled) return FATES.stalled;

    /* ── 3. 溃散（编年史期间压力涨满）──
       对应来古士的「黑潮」：那股一直在侵蚀的力量终于越过了文明的极限。
       ⚠️ 排在一切之前 —— 一个被压垮的文明，不该再谈飞升还是分裂。

       ★ 2026-09-13：**按性格分派崩法**。
         压垮他们的那股力量是同一股，但倒下的姿势不一样 ——
         征战型打到最后、隐世型缩回去、商业型散伙。
         这正好兑现已有的设计：性格本来就决定他们**怎么做事**
         （实测征战型 69% 被压垮、和平型 21%，见 CLAUDE.md 第六节 6.5）。

       ⚠️ 三个变体只是**形状不同**，难度完全一样 ——
          分派发生在"已经被压垮"之后，不影响任何平衡数字。 */
    if (world.civCollapsed) {
      /* ★★ 2026-09-13：先看**是哪条压力线先崩的**，再看性格 ★★

         三条线（消耗 / 失衡 / 离心）各自涨、各自有崩点，
         `evolution.js` 在崩的那一刻记下了是哪条先到的（`c.burstLine`）。

           **消耗**先满 → 溃散   —— 资源见底，谁来都一样，撑不住就是撑不住
           **失衡**先满 → 退潮   —— 环境坏了，只能往里缩
           **离心**先满 → 看性格 —— ★ 只有"内部不和"这件事才分
                                    打 / 散 / 缩 / 垮，因为那才和脾气有关

         ⚠️ 为什么资源和环境不按性格分：
            它们是**客观的**。一颗星球的水抽干了，跟这个文明
            好不好战没有关系 —— 硬要按性格分，就是给随机结果
            编一个因果故事。

       ── 离心那格怎么分（沿用 2026-09-13 上午那版，实测调过的）──
         八个性格的出现率差得极远（实测 649 局：征战 28.2%、和平 28.5%、
         求知 24.7%，而隐世 2.8%、信仰 1.8%、工匠 4.2%）。
         一开始按"一个性格配一条崩法"写，实测出来是
         「燎原 22.7% · 退潮 0.5%」—— 退潮等于白做，玩家永远看不到。
         按"崩的那一刻会怎么做"合成三组之后，最少的也有约 6%。 */
      var bl = world.evo && world.evo.civ && world.evo.civ.burstLine;
      if (bl === 'drain') return FATES.collapse;
      if (bl === 'unbal') return FATES.recede;

      if (tk === 'war') return FATES.blaze;                    // 打到没有人还在记录
      if (tk === 'peace' || tk === 'hermit' || tk === 'faith') {
        return FATES.recede;                                   // 退回去，不向外了
      }
      if (tk === 'trade' || tk === 'nomad' || tk === 'craft') {
        return FATES.disperse;                                 // 各走各的
      }
      return FATES.collapse;               // 求知 + 取不到性格时的兜底
    }

    // ── 2. 寂灭 ──
    // 征战型文明会把阈值放宽 —— 一个四处开战的文明更容易把自己打没。
    // 这是有因果的，不是随机惩罚玩家。
    var line = (temper && temper.doomProne) ? 25 : 15;
    if (e.energy > 85 || e.atmo < line || e.water < line) return FATES.doom;
    // 走到文明了，但生态几乎没铺开 —— 文明建在空壳上
    if (reachedCiv && L.life < 0.3) return FATES.doom;
    /* ★ 2026-09-15：这一条**从 marks 那一档提上来的**。
       它说的是"文明在灾难中消失" —— 和上面两条一样是**没活下来**，
       不该混进下面那一层"活成什么样"里（那里现在归四属性管）。

       ⚠️ 它原来排在光暗值判定**之后**（在 marks 那一档里）。
          提上来之后，攒够「寂灭」倾向的文明**优先**报寂灭 ——
          和"本源烧穿了"平起平坐。这是有意的：
          倾向是文明自己一次次选出来的，比属性更能说明它怎么没的。 */
    if (CivEvents.markReached(world, 'fall')) return FATES.doom;

    /* ── 3. ★ 光暗值的两个**极端**（玩家刻意推出来的）──
       2026-09-15：这两条**从"主路"降级成"硬判据"，但一条都没删**。

       为什么留着：光暗值是玩家的**主通道**（决策 #92 —— 那道
       "点名要哪种性格"的岔路口删掉之后，投放元素是玩家唯一的手）。
       把光暗值推到 ±70 是**刻意、费力、看得见**的一件事，
       不该被四属性顺手抢走。
       ⚠️ 所以它们排在**属性层之前** —— 极端情况优先，其余交给属性。

       ⚠️ 原来这里还有「被观测 / 抵达」（判 `dropCount <= 2`）和
          「融合」（判光暗种都投过）。那两条 2026-09-15 移进属性层了 ——
          它们本来就是"活下来之后活成什么样"，正是属性该管的事。
          详见下面 `lifeAxis` 那一大段。 */
    /* ★★ 2026-09-23：加了**科技门槛**（用户报的）★★

       **用户的原话**：
       > 「我刚测了一局文化最高但是最后结局是飞升」
       > 「给第一个条件加上一个科技大于 3，这样即使触发了也**看起来不维护**，
       >   要不然有人会觉得**凭什么科技是 0 的结局也能去外太空**」

       ── 为什么该加 ──
       飞升是「脱离母星引力井」—— 那是文明能造出来的**最大的东西**。
       而光暗值只说得出"你想去"，说不出"你造得出来"。
       科技 0 的文明报飞升，读起来是**设定漏了**，不是奇迹。

       ⚠️ 用户提的是 3，**实测定成 2** —— 见下面对照表，3 会把这条路废掉。

       ── 门槛是量的，不是拍的（1857 局，正常玩法）──
         科技终值分布：最小 0 · 25% 0 · **中位 1** · 75% 2 · 最大 4

             科技 ≥ 1   73.6%
           ★ 科技 ≥ 2   43.4%    ← 选的这个
             科技 > 3    0.3%    ← 用户最初提的，等于把这条路堵死

       ★ 选 2 的意思：挡掉**「连工具都没有」的那两档**（科技 0/1 合起来占一半以上），
         但**不要求这是个科技文明** —— 想飞升的玩家仍然只要"造得出来"就够。

       ⚠️ 它和**另一条飞升路**（下面四属性层：科技最高 + 攒够「飞升」倾向）
          是两种不同的故事，别把它们看成重复：
            这一条 = **你把它推过了那道线**（观察者的手）
            那一条 = **他们自己一直在往上看**（文明自己的选择）

       ⚠️⚠️ 这条只影响**玩家真的把光暗值推到 +70** 的局 ⚠️⚠️
          正常玩法下光暗值恒在 0 附近，这条路**一次都不会触发** ——
          所以它对整体结局分布几乎没有影响，改的是那种玩法的体验。
          要重量的话：探针必须**真的投光种**（投一次 +8，每秒回落 0.096）。 */
    if (world.light >= 70 && L.life > 0.8 &&
        Civ.attrOf(civ).sci >= 2) return FATES.ascend;
    if (world.light <= -70) return FATES.split;

    /* ═══════════════════════════════════════════════════════════════
       ── 7. ★★★ 四属性层：活下来的文明，由四个数决定「活成什么样」★★★
       ═══════════════════════════════════════════════════════════════

       ★ 2026-09-15 用户拍板（问「四属性管哪一块」时选的**乙**，
         搭配「复用现有命运、不新写文案」的**甲**）。

       ── 这一层管什么、不管什么 ──

         前面 1~6 档是**死活**（世界破碎 / 收割 / 未成形 / 溃散一族 /
         寂灭 / 光暗值极端）—— 那些一条都没动。
         **这一段管的是"活下来之后，他们活成了什么样子"。**

       ── 为什么要有它（一条实测出来的毛病）──

         跑 1859 局的命运分布：**「被观测」一个人占 43%**。
         这跟当年「溃散占 52%」是**同一个毛病** —— 玩两局碰一局同一个结局。
         而底下 14 条命运加起来才 21%（抵达 0.9% / 共生 0.9% /
         轮回 0.8% / 遗落 0.7% / 静滞 0.7%）——
         **写了整套预兆画面和专属传记，玩家几乎见不到。**

         ★ 四属性插进来最值钱的位置就是这一大坨：同样是活下来，
           军事高的活成一种样子、文化高的活成另一种。详见 `lifeAxis`。

       ── ⚠️⚠️ 代价（用户是在知道的情况下选的）⚠️⚠️

         「被观测 / 抵达」的门槛原来是 `dropCount <= 2`（你几乎没干预），
         「融合」原来是"光种暗种都投过"，「静滞」原来是"压力从没上过 75 档"。
         现在它们**降级成方向内的选择器** ——
         意思是**你投不投元素，不再是它们的主路**，属性才是。

         ⚠️ 唯一保住的是**光暗值的两个极端**（上面第 3 档，≥+70 / ≤−70）：
            那是刻意、费力、看得见的一件事，不该被顺手抢走。

       ⚠️ `marks.fall`（攒出来的寂灭倾向）2026-09-15 **提到了 1~6 档**，
          和本源阈值那两条并排（见上面）—— 它是"没活下来"，
          不该混进"活成什么样"里。 */

    /* ── ★★ 观察者撒手 → 「静滞」（2026-09-21 用户拍板）★★

       ⚠️⚠️⚠️ 2026-09-22 晚：这一整块**搬到上面 1.7 档去了** ⚠️⚠️⚠️

       它原来就摆在这个位置（死活档之后、四属性层之前），
       理由是"一个被压垮或被从外面取走的世界报静滞是报错了"。
       **那条理由被推翻了** —— 见 1.7 档那段注释："
       锁"是**玩家亲手点的判决**，不是"世界后来出了什么事"。
       ⚠️ 这里只留个墓碑，**别再把它搬回来**。

       ⚠️ 它原来和 `desc` 严丝合缝：`FATES.stillness.desc` 写着
          「再没有新的条目，**记录一直写到观测结束**」——
          **那半句现在对不上了**：锁了就当场收尾（`LOCK_OMEN_SEC`）。
          ⚠️ 文案**没动**（改法拍板时用户没提这句）。
             静滞的自然路径（军事轴）依旧跑满全场，所以这句不算全错；
             但"锁"那一支确实提前断了。**哪天要改这句 desc，依据在这儿。** */
    /* ── ★★ 信仰专属：「仰止」（2026-09-22 用户拍板）★★

       唯一的入口：**信仰** + ⑧`seent`【现身】给的 `contact` 攒够。
       ⚠️ 不算概率、不做第二道筛选 —— 满足这两条就直接进（用户原话：
          「不是这样选的不触发不需要算概率」）。

       ⚠️⚠️ 位置：**排在四属性层之前** ——
          「他们把你当成了神」是一件比"四项属性谁最高"硬得多的事，
          让属性层再选一次就把这一条的意思抹掉了。

       ⚠️ 但**排在死活档之后**：
          破碎 / 收割 / 未成形 / 溃散 / 本源烧穿 / 光暗极端 ——
          那些都是"没活下来"或"世界本身出了事"，
          一个被压垮的世界报「仰止」是**报错了**。

       ⚠️⚠️ 2026-09-22 晚：和 `lockedFate`（1.7 档）的先后**反过来了** ⚠️⚠️
          原来是「仰止在前」（理由是"你出过手"比"你撒手了"更具体）。
          现在 `lockedFate` 提到了 1.7 档，**它在前面**。

          ⚠️ 实际影响≈0：两条**同时成立**这种情况现在**到不了** ——
             先成立的那一条会**当场把时间线切掉**（见 `evolution.js`
             的 `LOCK_OMEN_SEC`），后面那条没机会写进来。
             一个信仰文明先点【现身】就是仰止收尾，先点【合上这一页】
             就是静滞收尾，各走各的。
          ⚠️ 那为什么还要定先后：**确定性**。同一份档案不管算多少次、
             由谁算，答案必须一样。`_civEvents_test.js` 有一条断言守着。 */
    if (world.civ && world.civ.temper === 'faith' &&
        CivEvents.markReached(world, 'contact')) {
      return FATES.revere;
    }

    if (reachedCiv) {
      var pr = world.civ && world.civ.proto;
      var fm = world.civ && world.civ.form;
      var axis = lifeAxis(world);

      /* ── 科技最高的那一支：**他们一直在朝外看** ──
         求知、观测、记录天上 —— 最后要么看见了外面，要么离开。 */
      if (axis === 'sci') {
        /* ⚠️ 顺序是有讲究的（第 3 档已经把"光暗值 ≥+70"挑走了）：
             攒够「飞升」倾向   → 飞升
             ★ 攒够「联络」倾向 → 抵达     ← 2026-09-22 加
             攒够「被观测」倾向 → 被观测
             投过光种（= 你回应过他们）→ 抵达
             都不是 → 被观测（一直发信号，没人理）

         ★★ 为什么加 `contact` 这一条（2026-09-22 用户拍板）★★

           「被观测」和「抵达」本来是一对（见上面 191 行那段注释）：
               被观测 = 他们朝外面发信号，**没人理**
               抵达   = 同样发了信号，**你回了**
           但 ⑧`seent` 的【现身】（结果行写着「那边**有了回应**」）
           给的却是 `watched: 2` ⇒ 攒够就报「被观测」
           ⇒ **你回应了他们，结局说你没回应。正好反了。**

           修法是给【现身】换一个新 mark（`contact`），而不是把这一条
           往前挪 —— 往前挪会让**所有投过光种的世界**都优先走抵达，
           那是另一条通路（**投过光种**才走它），影响面太大。
           ⚠️ 2026-09-22：原来这里还举了「①`lookup` 的降下启示」当例子 ——
              ① 已经删掉了（和 ⑧ 是同一个拍子），所以只留"投过光种"这一条。

         ⚠️ `contact` 必须排在 `watched` **之前** ——
            排在后面的话，一个同时攒够两张券的世界还是会先报「被观测」，
            这条改动就等于没做。
         ⚠️ 反过来说：**排在 `watched` 之后是"看着像改了、其实没改"**，
            这正是本项目栽过好几次的那种坑（改了但行为不变）。 */
        if (CivEvents.markReached(world, 'rise'))    return FATES.ascend;
        if (CivEvents.markReached(world, 'contact')) return FATES.arrive;
        if (CivEvents.markReached(world, 'watched')) return FATES.watched;
        return (world.dropTally && world.dropTally.light) ? FATES.arrive
                                                          : FATES.watched;
      }

      /* ── 军事最高的那一支：**他们靠压住内部撑到最后** ──
         压住了 → 一切都定死在一个水平上（静滞）；
         没压住 → 裂成互不往来的几块（分裂）。

         ⚠️ 「静滞」原来的门槛是 `omenBand <= 4`（压力从没上过 75 档）。
            那个门槛 2026-09-15 去掉了 —— 它现在由**属性**决定。
            语义接得上：军事高的文明把一切压在一个水平上，
            本来就"不再变化"，正是静滞那句话。 */
      if (axis === 'mil') {
        return CivEvents.markReached(world, 'split') ? FATES.split
                                                     : FATES.stillness;
      }

      /* ── 生产最高的那一支：**他们一直在造东西** ──
         造到最后要么把整颗星球改成自己要的样子（培育），
         要么连"自己动手"这件事都交出去了 —— 而那一支还要看**文化**。 */
      if (axis === 'prod') {
        if (CivEvents.markReached(world, 'machine')) {
          /* ★★ 2026-09-22 用户拍板：同一个 mark，按**文化**切成两条 ★★

               文化 < 2.5  → 遗落   （人不见了，东西还在转）
               文化 ≥ 2.5  → 同行   （打过一场，最后两边站到了一块）

             ⚠️ 界为什么是 2.5（不是整数）、为什么读 `attrOf` 而不是
                `attrCapped` —— 都写在 `FATES.coexist` 上面那一段，改之前先读它。
             ⚠️ 这两条**入口完全一样**，差别只在这一个数 ——
                `_civEvents_test.js` 有一条断言把两边都跑一遍。
                少了那条断言的话，谁顺手把这个判断删掉都不报错，
                只会所有世界又都走遗落。 */
          var culNow = (world.civ && Civ.attrOf(world.civ).cul) || 0;
          return (culNow >= 2.5) ? FATES.coexist : FATES.machine;
        }
        return FATES.cultivate;
      }

      /* ── 文化最高的那一支：**他们一直在认自己是谁** ──
         认到最后要么长进生态里（共生），要么不同族群合成一体（融合）。
         ⚠️ 「共生」的门槛（植类/森林 + 生态 ≥0.5）留着 ——
            它由**身份**决定，是这一支里最具体的那条路。 */
      if (axis === 'cul') {
        return ((pr === 'plantae' || fm === 'forest') && L.life >= 0.5)
               ? FATES.symbiose : FATES.merge;
      }

      /* ── 四项拉不开 → 轮回 ──
         "几经兴衰，又回到起点" —— 一个哪一头都不突出的文明，
         本来就没有偏向，落在这条最合适。 */
      return FATES.cycle;
    }

    // ── 8. 轮回（没能走到文明的兜底）──
    return FATES.cycle;
  }

  /* ═══════════════════════════════════════════════════════════════════
     ★★ 四属性 → 「活法」（2026-09-15）★★
     ═══════════════════════════════════════════════════════════════════

     ── 怎么算 ──
     ① 先按 `ATTR_RULES.endCap`（10）**截顶** —— 用户的原话：
        「以默认 10 为满值，但是超过 10 了还是继续写 11、12，
          多的值**不参与结局设定**了，只是显示而已」
        ⚠️ 截顶**只在这里发生**，界面上照原样显示（11 就写 11）。
     ② 取最高的那一项，就是这一局的"活法"。
     ③ 两个例外都算「均衡」（→ 轮回）：
        · **最高的那项并列**（军事 7 科技 7 …）—— 没有偏向
        · **最高和最低差 ≤ `evenGap`** —— 四项本来就拉不开

     ── ⚠️⚠️ 别把它写成"选一个属性去加分" ⚠️⚠️
     这一层**只读**四个数，不改它们。改它们的地方只有三处：
     `Civ.roll`（起点）、`CivEvents.apply`（事件推拉）、
     `stepCiv`（文化的自然增长）—— 都在别处。

     ── ⚠️ 老存档 ──
     `Civ.attrOf` 会按"物种 + 性格"现算一份补上，
     所以 2026-09-15 之前存的档读出来也有值，不会全 0。
     ═══════════════════════════════════════════════════════════════════ */

  /** 四个数，各按 `endCap` 截顶（**只给结局用**，界面不截） */
  function attrCapped(world) {
    var a = Civ.attrOf(world.civ);
    var cap = Civ.ATTR_RULES.endCap;
    return {
      mil:  Math.min(a.mil,  cap),
      sci:  Math.min(a.sci,  cap),
      prod: Math.min(a.prod, cap),
      cul:  Math.min(a.cul,  cap)
    };
  }

  /**
   * 这一局的「活法」：`'mil'` / `'sci'` / `'prod'` / `'cul'` / `'even'`。
   * 前四个原样返回属性名，第五个是"四项拉不开"。
   */
  function lifeAxis(world) {
    var a = attrCapped(world);
    var ks = ['mil', 'sci', 'prod', 'cul'];
    var top = ks[0], low = a[ks[0]], tied = false;

    for (var i = 1; i < ks.length; i++) {
      if (a[ks[i]] > a[top]) { top = ks[i]; tied = false; }
      else if (a[ks[i]] === a[top]) tied = true;
      if (a[ks[i]] < low) low = a[ks[i]];
    }

    /* ⚠️ 两条例外都在这里落地 —— 顺序无所谓，结果一样。
       ⚠️ `tied` 的判定：后面出现**更大**的要把标记清掉，
          否则「3, 8, 8, 3」和「8, 3, 3, 3」会判成同一类。 */
    if (tied) return 'even';
    if (a[top] - low <= Civ.ATTR_RULES.evenGap) return 'even';
    return top;
  }

  /* ⚠️ 2026-09-17：`stdDev(world)` **删掉了** —— 全项目零调用（含测试）。
     要"四项本源拉不拉得开"看 `lifeAxis` 那边（它用的是 `evenGap`，不是标准差）。
     ★ 它和 `core/worldgen.js` 里被删的 `essenceStdDev` 是**同一件事的两份**，
       两份都是死的 —— 见到「两把尺子」就一起清。 */

  function hasTrait(world, t) {
    return world.traits.indexOf(t) >= 0;
  }

  /* ═══════════════════════════════════════════════════════════════
     ③ 世界传记 —— 按数值分档选句子
     ═══════════════════════════════════════════════════════════════ */

  /* 六个阶段的名字。
     ⚠️ 这是 `core/evolution.js` 里 STAGES 的一份**副本** ——
        正常不该复制，但 ending.js 排在 evolution.js 前面加载
        （evolution 要调 Ending.fate），反向引用会循环依赖。
        所以只能抄一份。改阶段名的时候两边都要改，
        `_civEvents_test.js` 里有一条断言专门比对两边是否一致。 */
  var STAGE_NAMES = ['混沌', '冷却', '海洋', '大陆', '生态', '文明'];

  /** 按数值落在哪一档，取对应的说法 */
  function pick(value, bands) {
    for (var i = 0; i < bands.length; i++) {
      if (value >= bands[i][0]) return bands[i][1];
    }
    return bands[bands.length - 1][1];
  }

  function energyPhrase(v) {
    return pick(v, [
      [75, '能量充沛而炽烈'],
      [55, '能量充沛而不暴烈'],
      [35, '能量温和'],
      [20, '能量稀薄'],
      [-1, '热量几近散尽']
    ]);
  }

  function waterPhrase(v) {
    return pick(v, [
      [70, '水之本源丰盈'],
      [40, '水脉尚存'],
      [20, '水汽稀薄'],
      [-1, '地表干涸']
    ]);
  }

  function oceanPhrase(layer) {
    return pick(layer, [
      [0.55, '海洋覆盖大半表面'],
      [0.25, '海洋铺开数片'],
      [-1, '水面零散']
    ]);
  }

  function landPhrase(layer) {
    return pick(layer, [
      [0.5, '大陆连成整片'],
      [0.22, '大陆逐渐浮现'],
      [-1, '仅少数陆块浮出']
    ]);
  }

  function lifePhrase(layer) {
    return pick(layer, [
      [0.7, '植被连成整片，动物随之出现'],
      [0.35, '绿意自海岸向内陆蔓延'],
      [-1, '植被稀疏，只在温湿处扎下根']
    ]);
  }

  /* ═══════════════════════════════════════════════════════════════
     「评定」那一句摘要用的短语（2026-09-13 加）
     ═══════════════════════════════════════════════════════════════
     结局面板的第一段是：
         ── 星球 ──
         评定：丰饶
         水之本源丰盈，大气稳定，生态覆盖广泛。   ← 这一句

     「能不能住」看三样：**水、大气、生态** ——
     能量和大地不在这里，因为它们决定的是"长出什么文明"，
     不是"住不住得下去"（见第七节 1 的评定表，那个是四项平均）。

     ⚠️⚠️ 为什么单独抽成一张表，而不是像上面那五个一样内联 ⚠️⚠️
         `energyPhrase` / `waterPhrase` / `oceanPhrase` / `landPhrase` /
         `lifePhrase` 这五个是**内联在函数里的字符串** ——
         它们**不在任何一张"文案表"里**，`_style_test.js` 收集不到，
         所以是**文风盲区**：改坏了没有测试会红。
         （CLAUDE.md 里专门记着这个坑，见「界面短提示曾经也是盲区」那段。）
         新加的这两组放进表里，至少**新文案**从此有人守。
         ⚠️ 但那五个老的仍然是盲区 —— 见第十节「已知的文风盲区」。
     ═══════════════════════════════════════════════════════════════ */
  var RATING_PHRASES = {
    atmo: [
      [70, '大气浓密'],
      [40, '大气稳定'],
      [20, '大气稀薄'],
      [-1, '大气几近于无']
    ],
    life: [
      [0.7,  '生态覆盖广泛'],
      [0.35, '生态铺开过半'],
      [0.12, '生态只在温湿处'],
      [-1,   '生态几近于无']
    ]
  };

  /** 「评定」下面那一句摘要 */
  function ratingLine(world) {
    var e = world.essence || {};
    var L = (world.evo && world.evo.layers) || {};
    var life = L.life || 0;
    /* ⚠️ 没长出生态的世界（卡在生态之前）生态层是 0 ——
       不能写「生态几近于无」那种"观察到的结论"的口吻，
       因为它压根没走到那一步。用"未成形"才对得上事实。 */
    var lifeTxt = (world.evo && world.evo.stage >= 4)
      ? pick(life, RATING_PHRASES.life)
      : '生态未成形';
    return waterPhrase(e.water || 0) + '，' +
           pick(e.atmo || 0, RATING_PHRASES.atmo) + '，' + lifeTxt;
  }


  /**
   * 文明那一段。
   *
   * ⚡ 修真彩蛋（`world.civ.isEgg`）走**另一条路**：整段直接从
   *    `Civ.ANOMALIES[0].desc` 读，不拼物种 / 形态 / 性格那三样
   *    （它一样都没有 —— 形态和性格都是 null）。
   *
   *    ★ 2026-09-16：那三条铁律里的「观察者必须困惑」作废了。
   *      这条线现在的调子由 `ANOMALIES[0].desc` 自己定（真写修仙世界），
   *      这里只负责把它拼到「文明在其上崛起：」后面。
   */
  function civPhrase(world) {
    var civ = world.civ;
    if (!civ || !civ.proto) return '文明在其上崛起';

    /* ⚡ 修真彩蛋：那一段描述**从 `Civ.byKey(proto).desc` 读**。
       ⚠️ 2026-09-12 之前它是硬写在这个函数里的一个字符串 ——
          那是个**文风盲区**：`_style_test.js` 只扫各种"文案表"，
          写在逻辑里的字符串收集不到，改坏了没有测试会红。
          挪进 civ.js 的 ANOMALIES 表之后就一起被守门了。

       ⚠️ 起手用句号，**不能再用冒号** —— 调用方已经在前面接了
          「文明在其上崛起：」，再起一个冒号就成了
          「崛起：观测到异常：」，一段里断两次。 */
    if (civ.isEgg) {
      var an = Civ.byKey(civ.proto);
      var txt = (an && an.desc) || '观测到异常';
      return txt.replace(/[，、,;；。！？!?]+$/, '');
    }

    var def = Civ.byKey(civ.proto);
    var form = civ.form ? Civ.formOf(civ.form) : null;
    var temper = civ.temper ? Civ.temperOf(civ.temper) : null;

    // 三句话：住在哪 → 长什么样 → 怎么运转
    /* ★★ 顺序：先物种、后形态、再性格 ★★
       ⚠️⚠️ 2026-09-13 改过顺序和连接词，起因是**拼接病句** ⚠️⚠️

       症状（用户报的）：
           虫类 × 海洋文明 → 「聚居点浮在洋面上…；巢群铺满地表」
                              ↑ 洋面            ↑ 地表       打架

       根因：**形态描述和物种描述都在回答"他们住在哪"**，
             两句并排（`；`）就成了互相矛盾的两个事实。
             实测 56 种组合里**有 35 种**读起来是打架的。

       改法两层：
         ① 形态描述的措辞改成**动作**（「聚居点浮在洋面上」→「他们**把**聚居点**建到了**洋面上」）——
            它就不再和物种争"他们天生住哪"，而是说"这一支后来把家安在了哪"。
            而且这是**如实描述**：形态本来就是玩家投放元素的结果，
            是文明阶段发生的事，不是天生的。
         ② 这里把 `；` 改成 `。`，各说各的一句，不再并排。

       顺序也跟着换成 **物种 → 形态 → 性格**：
          先"他们是什么"、再"他们把家安在哪"、最后"他们怎么做事"，
          和「文明诞生」面板的 原型 / 特质 / 倾向 三行同序。

       ⚠️ 8 条形态描述**同时**在面板上单独显示（「特质：…」那一行），
          所以新措辞必须**单看也通顺**，不能只照顾拼接。 */
    var parts = [];
    parts.push(def.desc);
    if (form) parts.push(form.desc);
    if (temper) parts.push(temper.desc);

    /* 去掉结尾标点，交给调用方接句号 —— 不然会拼出「……微妙的平衡，。」。
       ⚠️ 正则里**必须有 。！？** —— 原来只剥了逗号分号，
          只要某个 desc 以句号结尾就会拼出「……。；……」。 */
    return parts.join('。').replace(/[，、,;；。！？!?]+$/, '');
  }

  /**
   * 收尾那一句 —— **拉高尺度**。
   *
   * 用户给的规范里第三条：前面写具体的事，最后一句突然拉到宇宙 / 地质尺度。
   * 全篇的"那一下"就靠这一句。
   *
   * ⚠️ 只在**真的走到过文明**的世界才加 —— 没长出过文明的世界写
   *    "共计多少个周期"是假话，它根本没有什么东西在计时。
   * ⚠️ 周期数由 `CivLore.cyclesOf` **现算**，不是写死的。
   *    事件里的 slow 字段会改编年史时长，写死就对不上了。
   * ⚠️ 这一句会让传记变长。`_ending_test.js` 有 180 字的硬上限，别加过头。
   *
   * ═══════════════════════════════════════════════════════════════
   * ⚠️⚠️ 2026-09-17：**试过一句一形态的版本，当天下架了** ⚠️⚠️
   * ═══════════════════════════════════════════════════════════════
   *
   * ── 起因 ──
   * 用户发现「在宇宙的尺度上，这不过是一次呼吸」**每局一字不差**，
   * 是套路句。所以做了一版：按文明把家安在哪（形态）取一句地质收尾 ——
   * 洋面「洋流会把留下的东西摊平」、地底「岩层会重新合上」…共 8 条。
   *
   * ── 为什么下架（用户当场看出来的）──
   * **那 8 条全都默认了"文明已经没了"** —— 可是 17 个命运里
   * **12 个的文明还好好的**：
   *
   *     长存（一个都没少）      ×  「地表会重新把它盖住」     ← 完全反了
   *     共生（成了生态一部分）  ×  「洋流会把留下的东西摊平」
   *     培育（正在改造星球）    ×  「树比他们活得久，也先倒下」
   *     飞升（离开这颗星球）    ×  「树比他们活得久，也先倒下」
   *     ……（共 12 个）
   *
   * ⚠️⚠️ **根因：「文明还在不在」是命运决定的，不是形态决定的。**
   *      按形态分，等于偷偷假设了"所有文明最后都会没"。
   *      实测跑出来的矛盾是 `_tail-audit.js` 查的。
   *
   * ⚠️⚠️ **而且 886 条测试一条都没红** —— 机器判不了语义矛盾。
   *      要再动这一句，**先想清楚新写法怎么处理"文明还在"的那 12 个命运**，
   *      并且补一条机器守门（见 docs/DECISIONS.md 决策 #127）。
   *
   * ── 结论 ──
   * 用户 2026-09-17 拍板：**暂时没有更好的改法，先留着原句**。
   * 保留原句不是因为它好，是因为**换错比不换更糟**。
   */
  function scaleTail(world) {
    var c = world.evo && world.evo.civ;
    if (!c) return '';
    return '从第一处聚居点算起，共计' + CivLore.cnNum(CivLore.cyclesOf(c)) +
           '个周期。在宇宙的尺度上，这不过是一次呼吸。';
  }

  /**
   * 生成完整传记。
   *
   * ⚠️⚠️ 2026-09-13：**传记里不再写「此界评定：X。」** ⚠️⚠️
   *
   * 那句和结局面板「── 星球 ──」那一节的「评定　宜居」是**同一件事**，
   * 却在同一张卡片上隔了十几行出现两遍（用户报的）。
   *
   * 删掉之后**反而更合文风**：CLAUDE.md 要求"最后一句拉高尺度"，
   * 现在收尾的正好是 `scaleTail` 那句（"在宇宙的尺度上，这不过是一次呼吸"）——
   * 比一个分类标签冷得多。
   *
   * ⚠️ 有四种命运**不走通用模板**（世界破碎 / 未成形 / 溃散一族 / 早夭的寂灭），
   *    它们各自拼了一句评定，**四处都要删** —— 只删通用那条的话，
   *    早夭的世界照样重复。见下面每一处的说明。
   *
   * ⚠️ `rating` 参数从此**不再使用**，所以从签名里去掉了。
   *    评定本身没丢：它在结局面板上「评定」那一行，以及下面那句摘要
   *    （`ratingLine`）里，各出现一次。
   */
  function bio(world, f) {
    /* ⚠️⚠️ 引用编年史那一行的长度上限，**必须声明在函数顶部** ⚠️⚠️

       2026-09-14：原来它写在溃散那一支里面。加「收割」的时候要在
       另一支里用同一个数 —— 如果照着写第二遍 `var QUOTE_MAX = 30`，
       读起来像是可以的（`var` 会提升），但**提升的只有声明，
       赋值不提升**：先走收割那一支的话，读到的是 `undefined`，
       `last.length <= undefined` 恒为 false，于是**永远走兜底那一句**。

       那种错不报错、不报警 —— 只是引用永远不出现。
       和 ending.js 里 `tk` 那段注释记的是同一个坑（var 只提升声明）。
       所以两个分支共用**这一个**。

       ⚠️ 2026-09-16：**30 → 31**。同一天给编年史的结果行补了句号，
       有 9 条原本正好 30 字、能被引用的行变成了 31 字，不提上限就会被挤出去、
       退回兜底句。提 1 个字对传记总长（上限 240）没有影响。
       ★ 动这个数之前先量一遍行长分布（拿 `_eventline-scan.js` /
         `_selfcontained-scan.js` 那种扫描器，把全部结果行/历史行导出来量）——
         ⚠️ 2026-09-17：原来这里写的 `_add_period.js` **不存在**
         （全项目没有，git 历史里也没有）。
       别拍脑袋。 */
    var QUOTE_MAX = 31;

    // 世界破碎 走单独的写法 —— 它没走到文明，硬套模板会很怪
    if (f.key === 'rift') {
      return '此界开辟于混沌，' + energyPhrase(world.essence.energy) + '，' +
             waterPhrase(world.essence.water) + '。' +
             '演化推进途中，光暗两股力量在同一区域反复撕扯，' +
             '内部应力突破临界，地表出现贯通性裂缝。' +
             '观测到此界结构失稳，后续记录中断。';
    }

    /* 此界不成 —— 卡在某个阶段过不去，再没往前走过一步。
       ⚠️ 必须单独写，不能套通用模板：
          通用模板开头是"冷却后海洋覆盖大半表面，大陆逐渐浮现" ——
          但一颗卡在「冷却」阶段的世界**根本没有海洋**，
          写成那样就是假话。 */
    if (f.key === 'stalled') {
      var st = world.evo.stage;
      return '此界开辟于混沌，' + energyPhrase(world.essence.energy) + '，' +
             waterPhrase(world.essence.water) + '。' +
             '演化推进到「' + STAGE_NAMES[st] + '」阶段便再难往前，' +
             '此后结构长期停滞，未再出现新的变化。' +
             '观测到此界未能成形。';
    }

    /* 溃散 —— 文明在编年史写到一半（或刚写完）时被压力压垮。
       ⚠️ 单独写：它走到了文明阶段，前面那些"立城、冶炼"都是真的，
          套早夭的模板会把它写成一颗没长出东西的世界。

       ★ 2026-09-13：燎原 / 退潮 / 星散**共用这一段**。
         它们和溃散是同一个死法（压力越过极限），只是倒下的姿势不同 ——
         所以共享骨架（含引用编年史最后一行那半句），
         只有"怎么没的"那一句按 f.key 分派（见下面的 `how`）。

       ⚠️ 新加的三条**必须留着「压力」二字** ——
          _civEvents_test.js 拿 /溃散|压力|散掉/ 认这条路，
          改没了那条断言会红。 */
    if (f.key === 'collapse' || f.key === 'blaze' ||
        f.key === 'recede'   || f.key === 'disperse') {
      var rows = (world.evo.civ && world.evo.civ.rows) || [];
      // 从编年史里挑一条"他们做过的事"放进传记 —— 让每个文明的传记不一样
      var deeds = [];
      for (var i = 0; i < rows.length; i++) {
        if (rows[i].kind === 'line' || rows[i].kind === 'deed') deeds.push(rows[i].text);
      }
      var last = deeds.length ? deeds[deeds.length - 1] : '他们一直在做该做的事';

      /* ⚠️ 引用前先剥掉行尾的标点 ——
         编年史的行本身是以「。」收尾的，直接塞进「」里会拼出
         「……一百二十七种。」。**两个句号**。这是批 2 把历史行
         改成完整句子之后才出现的（旧的通用行不带句号，所以看不出来）。
         剥掉之后由外面那个句号收尾，正好。 */
      last = last.replace(/[，、,;；。！？!?]+$/, '');

      /* ⚠️⚠️ 引用有长度上限 —— 这是**唯一会撑破 240 字**的地方 ⚠️⚠️

         编年史行有长有短（最长的一条 40 字，还含算出来的周期数），
         而溃散结局会把**最后一行整句嵌进传记里**。
         实测：人类那行「他们建起了不承担居住功能的结构。最高的那座
         花了三十代人。」把传记顶到了 **241 字**（上限 240）——
         跑 300 局是碰不到的，跑 1500 局才浮出来一次。

         为什么是"退让"而不是"删掉长行"：
         行长度是写作的自由度，不该被结局模板绑死；
         而引用只是锦上添花，没有它传记照样成立。
         **宁可少引一句，也不能超上限。** */
      var quoted = last.length <= QUOTE_MAX
        ? '观测记录的最后一条是：「' + last + '」。'
        : '观测在此处中断。';

      /* ★ 倒下那一下 —— 按崩法分派，四句等长（都是二十来字），
         所以四条路的传记长度几乎一样，不用分开盯 240 字上限（见下）。

         ⚠️⚠️ 2026-09-15 修：`recede` 和 `disperse` 这两句里原来**各多一个「此后」**，
            而下面那句的前缀就是「此后压力越过了它能承受的限度，」——
            拼出来是：
              「此后压力越过了它能承受的限度，他们退回边界以内，**此后**再没有向外…」
            正是 2026-09-13 修过的那一类毛病（两个「此后」挨着），
            **只是长在另一个地方** —— 那次修的是 desc，这次是传记里写死的这半句。

         怎么发现的：`_ending_test.js` 那条断言 2026-09-15 从"查 desc 里有没有「此后」"
            改成了"**直接数生成出来的传记**"。改完当场就红了这两条 ——
            而旧的代理指标查了一整年都没看出来（因为 desc 里确实没有）。 */
      var how;
      if (f.key === 'blaze') {
        how = '冲突没有被任何一方终止，各处的中断落在同一个周期';
      } else if (f.key === 'recede') {
        how = '他们退回边界以内，再没有向外';
      } else if (f.key === 'disperse') {
        how = '维系彼此之间的往来先断了，各处互不相干';
      } else {
        how = '结构失稳，文明在自身的重量下散掉';
      }

      /* ⚠️ 这一段是**整个游戏里最长的传记**，盯着 240 字上限（见
         _style_test.js 的 D 段）。原本这里是两句（约 51 字），说的
         是同一件事 —— 压到一句半，省下十来个字当余量。 */
      return '此界开辟于混沌，' + energyPhrase(world.essence.energy) + '，' +
             waterPhrase(world.essence.water) + '。' +
             '文明在其上崛起：' + civPhrase(world) + '。' +
             quoted +
             '此后压力越过了它能承受的限度，' + how + '，记录到此处中断。' +
             scaleTail(world);
    }

    /* 收割 —— 界外之物入侵，而观察者没有出手（2026-09-14）。
       ⚠️ 单独写：它的毁灭也发生在**编年史中途**（`stepCiv` 一看见
          `world.invaded` 就把它推进了预兆期），通用模板那套
          「此后，X」读起来像这一局正常走完了。 */
    if (f.key === 'harvest') {
      var hrows = (world.evo.civ && world.evo.civ.rows) || [];

      /* ⚠️⚠️ 这里取的是**整个编年史的最后一行**，不挑 kind ⚠️⚠️
         溃散那一支挑（只要 line / deed），是因为它的最后一行可能是
         征兆或纪元标题，那些不适合放进引号里。
         而这一条的最后一行十有八九是 `act` —— **观察者自己下的那一手**
         （「无视」的编年史行）。那正是这条命运的意思：
         **记录是在他选择不动之后的下一行断掉的。**
         照抄溃散那个挑法，会刚好把最要紧的一行跳过去。 */
      var hlast = hrows.length ? hrows[hrows.length - 1].text : '';
      hlast = hlast.replace(/[，、,;；。！？!?]+$/, '');
      var hquote = (hlast && hlast.length <= QUOTE_MAX)
        ? '观测记录的最后一条是：「' + hlast + '」。'
        : '观测在此处中断。';

      /* ★ 这一条**不写「共计多少个周期」**，是有意的 ⚠️

         （写这一支的时候还查出一个**真 bug**，顺手修了：`cyclesOf`
           原来算的是**编年史全长**，而被切断的文明没活到那时候 ——
           最坏虚报三成。现在 `evolution.js` 在切断那一刻记下
           `c.cutAt`，`cyclesOf` 报实际活到的长度。详见 civLore.js。）

         所以这里不写数字**不再是因为会撒谎**，而是因为收尾句换成了
         更狠的那一句：「宇宙里这样的事，数不胜数。」
         它照样是「拉高尺度」（文风规范第五条）。

         ⚠️ 数本身没有丢：结局面板「存活周期」那一行还在（而且现在是真的）。 */
      return '此界开辟于混沌，' + energyPhrase(world.essence.energy) + '，' +
             waterPhrase(world.essence.water) + '。' +
             '文明在其上崛起：' + civPhrase(world) + '。' +
             hquote +
             '此后有东西从记录之外进来，文明停止了运转。' +
             '它们没有留下原因，也没有留下目的。' +
             '宇宙里这样的事，数不胜数。';
    }

    /* ═══ ★★ 修真彩蛋两个专属结局的传记（2026-09-16）★★ ═══

       两条都**单独写**，不套通用模板 —— 通用模板的收尾是
       `'此后，' + f.desc`，而这两条一条是"中途被切断"（写"此后"像这一局
       正常走完了）、一条的 desc 里带着时间词（拼出来两个时间词顶在一起）。

       ⚠️ 两条的骨架一样（开头 → 冷却 → 生态 → 文明段 → 收尾 → 拉高尺度），
          只有倒数第二句成对：**一灭一存**。
          ——这是这对结局在文本上的身份证，改的时候两条一起改。 */

    /* 劫灭：压力爆了，编年史写到一半断掉。
       ⚠️ 和溃散一族同构：引用编年史最后一行，而且**要挑 kind**
          （它的最后一行可能是征兆或纪元标题，那些不适合放进引号里）。
          ⚠️ `QUOTE_MAX` 用的是函数顶部那一个，别在这里再声明一遍
             （`var` 只提升声明 —— 见那个变量上面的血泪注释）。 */
    if (f.key === 'xiuDoom') {
      var xrows = (world.evo.civ && world.evo.civ.rows) || [];
      var xtail = '';
      for (var xr = 0; xr < xrows.length; xr++) {
        if (xrows[xr].kind === 'line' || xrows[xr].kind === 'deed') {
          xtail = xrows[xr].text;
        }
      }
      xtail = xtail.replace(/[，、,;；。！？!?]+$/, '');
      var xquote = (xtail && xtail.length <= QUOTE_MAX)
        ? '观测记录的最后一条是：「' + xtail + '」。'
        : '观测在此处中断。';

      return '此界开辟于混沌，' + energyPhrase(world.essence.energy) + '，' +
             waterPhrase(world.essence.water) + '。' +
             '文明在其上崛起：' + civPhrase(world) + '。' +
             xquote +
             '最后一次落下来的东西，他们没有接住。记录到此处中断。' +
             scaleTail(world);
    }

    /* 长存：撑到了最后。 */
    if (f.key === 'xiuFlourish') {
      var yL = world.evo.layers;
      return '此界开辟于混沌，' + energyPhrase(world.essence.energy) + '，' +
             waterPhrase(world.essence.water) + '。' +
             '冷却后' + oceanPhrase(yL.ocean) + '，' + landPhrase(yL.continent) + '。' +
             (yL.life > 0 ? '生态诞生后，' + lifePhrase(yL.life) + '。' : '') +
             '文明在其上崛起：' + civPhrase(world) + '。' +
             '往后每一次落下来的东西，他们都接住了。' +
             scaleTail(world);
    }

    /* ═══ ★★ 仰止 —— 信仰专属（2026-09-22）★★ ═══

       ⚠️ **单独写**，不套通用模板 —— 和修真那两条同一个理由：
          通用模板的收尾是「{f.desc}。」，而这一条的 desc 只有一句
          （「文明把你当成了神…」），撑不起用户要的那个**麻木感**。

       ── 用户定的两条，逐条落在哪里 ──
         ① 「观测者被当成神崇拜，人民的信仰统一了，**但这个信仰也被宗教扭曲了**」
            → 「你不再开口，你的神谕却布满大地。」
              ★ **扭曲就在「却」那一下**：满地的神谕打着你的名义，
                而你一个字都没说。不写"扭曲"，只写"神谕"满天飞。
         ② 「这不是个好结局，所以需要**制造麻木感**」
            → 「一切事物都有了解释，」
              ★ 麻木**不是"不动了"**（那是静滞），是**不再有问题**。
                一切都解释得通，所以没有人再想。
            → 「再也没有人仰望星空。」
              ★ 这一句是给**删掉的 ①`lookup`「{folk}开始仰望」**收口的：
                那个文明是从"第一次抬起头"开始的，到这个结局里
                **抬头这件事本身没有了**。和「仰止」的「止」是同一个动作。

       ⚠️ 人称（2026-09-22 用户改）：这一条**破例用「你」** ——
          别的命运都是第三人称旁观，只有"你被当成神"这一条当面说。
          ⚠️ 全篇仍然**不出现「他 / 她」**（观测者没有性别）。

       ⚠️⚠️ **长度只能量，不能估 —— 这条踩过两次** ⚠️⚠️
             第一次：估「约 195 字」，实测 **272**（上限 240）。
             第二次（2026-09-22）：照「骨架 + 收尾 = 169 字」推出「中段可用
               60 字」，实际骨架最长 **183 字**（一万六千局），中段上限只有
               **57 字**。写进去之后最长一片 **245 字**，**而
               `_style_test.js` 照样报绿**（它报的 236 是**别的命运**）——
               那条断言量的是**自然跑出来的结局**，仰止出现率不到 1%，
               800 局里一颗都没抽到 ⇒ **这条传记当时等于没人看。**
             ⇒ 教训不只是"抽样"：**只跑自然结局，对"有自己的 bio 分支"的
               命运就是盲区**，必须主动构造出来量。守门现在在
               `_style_test.js` 的 D 段 —— 对每颗世界强行算一遍仰止的传记。

             ⇒ 中段 **52 字**，最长 **235 字**，**只剩 5 字余量**。
                ⚠️ 想再往这段里加字，先看这条注释 —— 顶到 240 就超了。

       ★ 四条意思（2026-09-22 用户定稿）：
           信号得到了回应，回应他们的是你。              ← 被当成神（★ 当面说）
           你不再开口，你的神谕却布满大地。              ← ★ 扭曲
           一切事物都有了解释，                          ← ★ 麻木感
           再也没有人仰望星空。                          ← ★「仰止」的「止」
       （更早还砍掉过「那个方向上建起了东西，越建越高」那句画面。） */
    if (f.key === 'revere') {
      var vL = world.evo.layers;
      return '此界开辟于混沌，' + energyPhrase(world.essence.energy) + '，' +
             waterPhrase(world.essence.water) + '。' +
             '冷却后' + oceanPhrase(vL.ocean) + '，' + landPhrase(vL.continent) + '。' +
             (vL.life > 0 ? '生态诞生后，' + lifePhrase(vL.life) + '。' : '') +
             '文明在其上崛起：' + civPhrase(world) + '。' +
             '信号得到了回应，回应他们的是你。' +
             '你不再开口，你的神谕却布满大地。' +
             '一切事物都有了解释，再也没有人仰望星空。' +
             scaleTail(world);
    }

    // 寂灭 也早夭，但它是环境崩了，不是世界碎了
    if (f.key === 'doom' && world.evo.stage < 5) {
      return '此界开辟于混沌，' + energyPhrase(world.essence.energy) + '，' +
             waterPhrase(world.essence.water) + '。' +
             '冷却后' + oceanPhrase(world.evo.layers.ocean) + '，' +
             landPhrase(world.evo.layers.continent) + '。' +
             '此后环境持续恶化，生态未能延续。';
    }

    var L = world.evo.layers;
    var parts = [];

    parts.push('此界开辟于混沌，' + energyPhrase(world.essence.energy) + '，' +
               waterPhrase(world.essence.water) + '。');

    parts.push('冷却后' + oceanPhrase(L.ocean) + '，' + landPhrase(L.continent) + '。');

    if (L.life > 0) {
      parts.push('生态诞生后，' + lifePhrase(L.life) + '。');
    }

    if (world.evo.stage >= 4) {   // 走到过生态阶段之后才写文明段
      parts.push('文明在其上崛起：' + civPhrase(world) + '。');
    }

    /* 命运那一段（飞升 / 分裂 / 融合 / 被观测 / 轮回 / 寂灭）
       ★ 2026-09-17：**前缀「此后，」删掉了** —— 原来是
          `'此后，' + f.desc + '。'`，七条命运全都长成一个开头。

       ⚠️ 为什么删：用户给的新版文案规则里有一条「句号后不接连词」，
          「此后」正是那种词。删掉之后直接接命运描述，
          上下文的先后关系靠命运描述自己的时态交代，反而更冷。

       ⚠️ 顺带解掉一个老包袱：原来 `f.desc` 里**不许出现「此后」**，
          否则会拼成「此后，……。此后再没有新的条目」（2026-09-13 四条全踩）。
          前缀没了，这个约束自然失效 —— 但 desc 里的「此后」**一个字没动**，
          它们现在是正文的一部分，读起来是对的。 */
    parts.push(f.desc + '。');

    /* ★ 拉高尺度 —— 全篇的"那一下"，只有真的长过文明的世界才有。
       ⚠️ 它必须**收尾**：这里原本后面还跟着一句「此界评定：X。」，
          删掉之后这句才真正落到最后（见 bio 函数头那段说明）。 */
    parts.push(scaleTail(world));

    return parts.join('');
  }

  /* ═══════════════════════════════════════════════════════════════
     对外入口
     ═══════════════════════════════════════════════════════════════ */

  /**
   * 给一颗世界出结案报告。
   *
   * @param {object} world
   * @returns {object} {
   *   rating:     { key, name },   世界评定
   *   ratingLine: '……',            评定下面那句摘要
   *   fate:       { key, name },   结局分支
   *   bio:        '……',            传记正文
   *   civ:        { title, name, form, temper, isEgg } | null  文明信息
   *              （没走到文明就是 null；title 是玩家起的名，name 是物种）
   * }
   * ⚠️ 结局那个字段叫 **`fate`**，不叫 `branch` ——
   *    这份说明以前写成 `branch`，照着写 `result.branch` 会永远拿到
   *    `undefined`，而且**不报错**。
   */
  function evaluate(world) {
    var r = rate(world);
    var reachedCiv = world.evo.stage >= 5;

    /* ⚠️ 命运**优先用预兆期定下的那个**。
       文明走到「命运预兆」时已经算过一次（见 evolution.js 的 stepCiv），
       存进了 world.evo.civ.fate。这里再算一遍的话，
       光暗值在预兆期里已经衰减了 20~30 秒，结果可能变 ——
       于是会出现"预兆动画演的是飞升，结局面板说是轮回"。
       那种 bug 看起来像游戏坏了，很难查。 */
    var f = (world.evo && world.evo.civ && world.evo.civ.fate)
          ? world.evo.civ.fate
          : fate(world, reachedCiv);

    var civ = world.civ && world.civ.proto ? {
      // 文明自己的名字，比如「潮汐议会」。
      // ⚠️ 必须走 displayName —— 玩家可能自己在诞生面板上起过名，
      //    civName 只是"没起名时的推荐值"。
      title: Civ.displayName(world),
      name: world.civ.protoName,                 // 物种（原型）：兽类 / 虫类…
      form: world.civ.formName || null,          // 形态（特质）：大陆文明 / 海洋文明…
      temper: world.civ.temperName || null,      // 性格（倾向）：和平 / 征战 / 求知
      isEgg: !!world.civ.isEgg
    } : null;

    return {
      rating: r,
      ratingLine: ratingLine(world),   // ★ 评定下面那句摘要
      fate: f,
      bio: bio(world, f),
      civ: civ
    };
  }

  /* ⚠️ 2026-09-17：`headline(result)` **删掉了** —— 全项目零调用（含测试）。
     它是**为一个还没做的功能够写的**（分享卡片）。真要做得时候，
     它会返回 `rating.name + ' · ' + fate.name`，五行，随手就能写回来 ——
     但那之前它只是躺在包里没人读的代码。
     ⚠️ 分享卡片这件事的状态见 `app.js` 里「将来做分享卡片要用」那一行，
       以及 `docs/DECISIONS.md` 决策 #6。 */

  return {
    FATES: FATES,
    STAGE_NAMES: STAGE_NAMES,
    rate: rate,
    fate: fate,
    /* ★ 2026-09-22：结局被事件链**钉死**了吗 ——
       `evolution.js` 靠它当场收尾（见 `LOCK_OMEN_SEC` 那一段）。
       ⚠️ 和 `fate()` 是**同一把尺子**：它内部会拿 `fate()` 自检一遍，
          对不上就返回 null（宁可晚收尾，不锁错）。 */
    lockedFate: lockedFate,
    /* ★ 2026-09-15：四属性那一层单独导出去 ——
       `_ending_test.js` 要直接喂它一组数，验"哪个属性最高就落到哪条命运"
       和"并列/拉不开都走轮回"。**不导出的话只能靠跑几千局去撞**，
       撞不出「均衡」那条就永远没人守。 */
    lifeAxis: lifeAxis,
    attrCapped: attrCapped,
    bio: bio,
    evaluate: evaluate,
    /* ⚠️ 2026-09-17：`headline` / `stdDev` 两个导出删了 ——
       真死代码（全项目零调用，含测试）。见上面各自那一段说明。 */
    /* ⚠️ 2026-09-13 导出的，**只为测试** ——
       「文明在其上崛起：…」那一段的拼接方式（顺序 + 连接词）
       是"拼接病句"那次修复的关键，得有断言钉住它。
       走 `bio()` 只能查整篇，查不出"顺序对不对、用的是句号还是分号"。
       见 `_ending_test.js` 的 J 段。 */
    civPhrase: civPhrase,
    /* ★ 同样只为测试：`_style_test.js` 要逐条扫这些短语。
       不导出的话它们就落进"内联字符串"那个文风盲区了
       （energyPhrase 那五个就是这么变成盲区的）。 */
    RATING_PHRASES: RATING_PHRASES,
    ratingLine: ratingLine
  };
});
